---
phase: 00-admin-saas-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/admin.routes.ts
autonomous: true
requirements:
  - TENANT-02

must_haves:
  truths:
    - "All document review routes use requireSuperAdmin middleware directly"
    - "No inline role checks exist in admin.routes.ts document section"
    - "CNPJ lookup is already protected (verify only)"
  artifacts:
    - path: "server/routes/admin.routes.ts"
      provides: "Consistent middleware usage on all admin routes"
      contains: "requireSuperAdmin"
  key_links:
    - from: "server/routes/admin.routes.ts"
      to: "server/auth.ts"
      via: "import { requireSuperAdmin }"
      pattern: "requireSuperAdmin"
---

<objective>
Normalize 3 document routes in admin.routes.ts to use requireSuperAdmin middleware instead of requireAuth + inline role check.

Purpose: Inconsistent middleware makes security auditing impossible. Three routes use requireAuth + inline `if (role !== "superadmin")` instead of the proper requireSuperAdmin middleware. This is a TENANT-01 concern (consistent access control) and confirms TENANT-02 (CNPJ already protected).
Output: admin.routes.ts with consistent middleware on all routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-admin-saas-foundation/00-RESEARCH.md

<interfaces>
From server/auth.ts (already imported in admin.routes.ts):
```typescript
import { requireAuth, requireSuperAdmin } from "../auth";
// requireSuperAdmin checks req.session.role === "superadmin"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace inline role checks with requireSuperAdmin on 3 document routes</name>
  <files>server/routes/admin.routes.ts</files>
  <read_first>
    - server/routes/admin.routes.ts (lines 415-470 — the 3 document routes with inline role checks)
    - server/auth.ts (to confirm requireSuperAdmin signature)
  </read_first>
  <action>
Three routes at lines 418, 440, 454 of admin.routes.ts use `requireAuth` + inline `if (req.session.role !== "superadmin")` check. Replace each with `requireSuperAdmin` and remove the inline check.

**Route 1 — PATCH document status (line 418):**
Change: `router.patch("/api/admin/providers/:id/documents/:docId/status", requireAuth, async (req, res) => {`
To: `router.patch("/api/admin/providers/:id/documents/:docId/status", requireSuperAdmin, async (req, res) => {`
Remove the inner block (lines 420-422):
```typescript
      if (req.session.role !== "superadmin") {
        return res.status(403).json({ message: "Apenas superadmin pode revisar documentos" });
      }
```

**Route 2 — GET documents list (line 440):**
Change: `router.get("/api/admin/providers/:id/documents", requireAuth, async (req, res) => {`
To: `router.get("/api/admin/providers/:id/documents", requireSuperAdmin, async (req, res) => {`
Remove the inner block (lines 442-444):
```typescript
      if (req.session.role !== "superadmin") {
        return res.status(403).json({ message: "Acesso negado" });
      }
```

**Route 3 — GET document download (line 454):**
Change: `router.get("/api/admin/providers/:id/documents/:docId/download", requireAuth, async (req, res) => {`
To: `router.get("/api/admin/providers/:id/documents/:docId/download", requireSuperAdmin, async (req, res) => {`
Remove the inner block (lines 456-458):
```typescript
      if (req.session.role !== "superadmin") {
        return res.status(403).json({ message: "Acesso negado" });
      }
```

Do NOT change any other routes. The rest of admin.routes.ts already uses requireSuperAdmin correctly.

Also verify CNPJ endpoint (line 47) already has `requireSuperAdmin` — it does per research. Just confirm visually and note in summary (TENANT-02 already satisfied).
  </action>
  <verify>
    <automated>cd C:/ClaudeCode/Consulta_ISP && grep -n "requireAuth" server/routes/admin.routes.ts && echo "---" && grep -c 'role !== "superadmin"' server/routes/admin.routes.ts && echo "---" && grep -c "requireSuperAdmin" server/routes/admin.routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/routes/admin.routes.ts has ZERO occurrences of `requireAuth` (the import of requireAuth can be removed too if no other route uses it — check first)
    - server/routes/admin.routes.ts has ZERO occurrences of inline `req.session.role !== "superadmin"` checks
    - The 3 document routes use `requireSuperAdmin` as middleware parameter
    - All other routes remain unchanged
    - CNPJ route (line ~47) still has `requireSuperAdmin`
    - File compiles without TypeScript errors
  </acceptance_criteria>
  <done>All admin routes consistently use requireSuperAdmin middleware. No inline role checks remain. TENANT-02 confirmed (CNPJ already protected).</done>
</task>

</tasks>

<verification>
- `grep "requireAuth" server/routes/admin.routes.ts` returns nothing or only the import line (if other routes still need it)
- `grep 'role !== "superadmin"' server/routes/admin.routes.ts` returns nothing
- `grep -c "requireSuperAdmin" server/routes/admin.routes.ts` shows count increased (was ~20, now ~23)
</verification>

<success_criteria>
- Zero inline role checks in admin.routes.ts
- All 3 document routes use requireSuperAdmin directly
- CNPJ endpoint confirmed protected (TENANT-02)
- No functional changes to route behavior (same 403 for non-superadmin)
</success_criteria>

<output>
After completion, create `.planning/phases/00-admin-saas-foundation/00-03-SUMMARY.md`
</output>
