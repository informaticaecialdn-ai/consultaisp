---
phase: 03-remocao-sync-centralizado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/erp.routes.ts
  - server/storage/customers.storage.ts
  - server/storage/index.ts
autonomous: true
requirements: [NOSYNC-01, NOSYNC-02, NOSYNC-03]

must_haves:
  truths:
    - "No endpoint exists that bulk-syncs ERP customer data into the customers table"
    - "The syncErpCustomers method no longer exists in storage layer"
    - "CSV import and manual createCustomer still work (own-provider only)"
    - "ERP test-connection and config endpoints are untouched"
  artifacts:
    - path: "server/routes/erp.routes.ts"
      provides: "ERP routes WITHOUT the sync endpoint"
      must_not_contain: "syncErpCustomers"
    - path: "server/storage/customers.storage.ts"
      provides: "Customer storage with only own-provider operations"
      must_not_contain: "syncErpCustomers"
    - path: "server/storage/index.ts"
      provides: "Storage interface without syncErpCustomers"
      must_not_contain: "syncErpCustomers"
  key_links:
    - from: "server/routes/import.routes.ts"
      to: "storage.createCustomer"
      via: "CSV import uses createCustomer (must still work)"
      pattern: "createCustomer"
    - from: "server/routes/erp.routes.ts"
      to: "connector.testConnection"
      via: "ERP test endpoint still works"
      pattern: "testConnection"
---

<objective>
Remove all ERP-to-customers sync logic so the customers table only contains own-provider data (CSV import or manual creation).

Purpose: Phase 2 made queries real-time against ERPs. The old sync endpoint that fetched ERP delinquents and upserted them into the customers table is now unnecessary and violates the "no cross-provider storage" principle.

Output: Clean codebase where the only write paths to the customers table are createCustomer (used by CSV import and manual creation), both scoped to the session's providerId.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

Key existing interfaces (extracted from codebase):

From server/storage/index.ts (lines to REMOVE):
```typescript
// Interface method (line 145):
syncErpCustomers(providerId: number, erpSource: string, customersData: any[]): Promise<{ upserted: number; errors: number }>;

// Implementation delegation (line 216):
syncErpCustomers = (providerId: number, erpSource: string, customersData: any[]) => this._customers.syncErpCustomers(providerId, erpSource, customersData);
```

From server/routes/erp.routes.ts (lines 68-109 to REMOVE):
```typescript
router.post("/api/provider/erp-integrations/:source/sync", requireAuth, async (req, res) => {
  // ... fetches from ERP, calls storage.syncErpCustomers, logs sync
});
```

From server/storage/customers.storage.ts (lines 54-133 to REMOVE):
```typescript
async syncErpCustomers(providerId: number, erpSource: string, customersData: any[]): Promise<{ upserted: number; errors: number }> {
  // ... upserts customer data from ERP into customers table
}
```

KEEP INTACT:
- server/routes/import.routes.ts (CSV import uses createCustomer -- own provider only)
- customers.storage.ts: getCustomersByProvider, getCustomerByCpfCnpj, createCustomer
- erp.routes.ts: GET /api/erp/available, GET /api/erp/config-fields/:source, GET/PUT /api/provider/erp-integrations, POST test-connection
- erp.routes.ts: GET /api/provider/erp-sync-logs (keep for historical log viewing)
- server/heatmap-cache.ts scheduler (unrelated, do NOT touch)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove sync endpoint and syncErpCustomers storage method</name>
  <files>server/routes/erp.routes.ts, server/storage/customers.storage.ts, server/storage/index.ts</files>
  <read_first>
    - server/routes/erp.routes.ts (full file -- identify exact line range of POST sync endpoint)
    - server/storage/customers.storage.ts (full file -- identify syncErpCustomers method)
    - server/storage/index.ts (find syncErpCustomers in interface and implementation)
  </read_first>
  <action>
    1. In server/routes/erp.routes.ts: DELETE the entire `router.post("/api/provider/erp-integrations/:source/sync", ...)` handler (lines 68-109). Keep all other routes (GET integrations, PUT config, POST test-connection, GET sync-logs). Per NOSYNC-01: no sync endpoint should exist. Per NOSYNC-02: no upsert of cross-provider data.

    2. In server/storage/customers.storage.ts: DELETE the entire `syncErpCustomers` method (lines 54-133). Keep getCustomersByProvider, getCustomerByCpfCnpj, getCustomersByAddressHash, getCustomersByExactAddress, createCustomer. Per NOSYNC-02.

    3. In server/storage/index.ts: Remove `syncErpCustomers` from the IStorage interface (line 145) and from the DatabaseStorage class implementation delegation (line 216). Per NOSYNC-02.

    4. After removals, check if any unused imports remain in erp.routes.ts (the sync endpoint used storage.syncErpCustomers, storage.createErpSyncLog, storage.upsertErpIntegration, storage.incrementErpIntegrationCounters -- some of these are still used by other routes, only remove if truly orphaned).

    DO NOT touch:
    - server/routes/import.routes.ts (CSV import is own-provider, must stay per NOSYNC-03)
    - server/heatmap-cache.ts (unrelated scheduler)
    - Any ERP connector fetchDelinquents/fetchCustomerByCpf methods (still used by real-time query engine)
    - The erp_sync_logs table or createErpSyncLog (historical data, keep for audit)
  </action>
  <verify>
    <automated>cd C:/ClaudeCode/Consulta_ISP && npx tsx --eval "import('./server/storage/customers.storage').then(m => { const cs = new m.CustomersStorage(); if ('syncErpCustomers' in cs) { process.exit(1); } console.log('syncErpCustomers removed'); })" && grep -c "syncErpCustomers" server/routes/erp.routes.ts server/storage/index.ts server/storage/customers.storage.ts 2>&1 | grep -v ":0$" && echo "FAIL: syncErpCustomers still referenced" && exit 1 || echo "PASS: no references remain"</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "syncErpCustomers" server/ returns zero matches
    - grep "erp-integrations/:source/sync" server/routes/erp.routes.ts returns zero matches
    - grep "createCustomer" server/storage/customers.storage.ts returns at least 1 match (method preserved)
    - grep "createCustomer" server/routes/import.routes.ts returns at least 1 match (CSV import preserved)
  </acceptance_criteria>
  <done>
    - The POST /api/provider/erp-integrations/:source/sync endpoint is gone
    - The syncErpCustomers method is gone from storage interface and implementation
    - No code path exists that bulk-writes ERP customer data into the customers table
    - CSV import (createCustomer) and manual creation are untouched
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build and remaining write paths to customers table</name>
  <files>server/routes/erp.routes.ts, server/storage/customers.storage.ts</files>
  <read_first>
    - server/storage/customers.storage.ts (confirm only safe methods remain)
    - server/routes/import.routes.ts (confirm CSV import still compiles)
  </read_first>
  <action>
    1. Run TypeScript compilation to confirm no broken imports or type errors after removal: `npx tsc --noEmit` (or the project's equivalent type check).

    2. Audit remaining write paths to the customers table by grepping for `insert(customers)` and `update(customers)` across the entire server directory. Confirm every remaining write is scoped to the session's own providerId (per NOSYNC-03):
       - createCustomer in customers.storage.ts: receives providerId parameter (used by CSV import which passes req.session.providerId)
       - Any update paths should also be provider-scoped

    3. If any OTHER write path to the customers table exists that could insert cross-provider data, flag it and remove it.

    4. Verify the dev server starts without errors: `npx tsx server/index.ts` (start and immediately kill after confirming no crash).
  </action>
  <verify>
    <automated>cd C:/ClaudeCode/Consulta_ISP && npx tsc --noEmit 2>&1 | head -20 && echo "TypeCheck complete"</automated>
  </verify>
  <acceptance_criteria>
    - npx tsc --noEmit exits with code 0 (no type errors)
    - grep -rn "insert(customers)" server/ shows only createCustomer in customers.storage.ts
    - grep -rn "update(customers)" server/ shows only provider-scoped updates (if any)
  </acceptance_criteria>
  <done>
    - TypeScript compiles cleanly after sync removal
    - All remaining write paths to customers table are own-provider scoped
    - The system is ready for Phase 4 (address search and migrators use real-time queries, not stored data)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "syncErpCustomers" server/` returns nothing
2. `grep -r "erp-integrations/:source/sync" server/` returns nothing
3. `npx tsc --noEmit` passes
4. `grep -rn "insert(customers)" server/` shows only createCustomer (own-provider)
5. CSV import endpoint still exists: `grep "api/import/customers" server/routes/import.routes.ts`
</verification>

<success_criteria>
- No sync mechanism writes ERP customer data into the customers table
- TypeScript builds cleanly
- CSV import and manual customer creation are functional (own-provider only)
- ERP connector test/config endpoints are untouched
</success_criteria>

<output>
After completion, create `.planning/phases/03-remocao-sync-centralizado/03-01-SUMMARY.md`
</output>
