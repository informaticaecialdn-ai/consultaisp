---
phase: 03-backend-modularization
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/provider.routes.ts
  - server/routes/erp.routes.ts
  - server/routes/admin.routes.ts
  - server/routes/financeiro.routes.ts
  - server/routes/credits.routes.ts
  - server/routes/chat.routes.ts
  - server/routes/ai.routes.ts
  - server/routes/public.routes.ts
autonomous: true
requirements: [MOD-02, MOD-04]

must_haves:
  truths:
    - "Provider routes handle profile, settings, users, partners, documents, webhook, n8n config with same behavior"
    - "ERP routes handle integrations CRUD, test connection, sync, sync logs with same behavior"
    - "Admin routes handle all superadmin operations (providers, users, chat, financial, erp-catalog) identically"
    - "Financial routes handle invoices, asaas integration, monthly generation, SaaS metrics"
    - "Credits routes handle purchase, orders, asaas PIX for both provider and admin"
    - "Chat routes handle support threads and visitor chats for both provider and admin"
    - "AI routes handle consultation and anti-fraud analysis with streaming"
    - "Public routes handle visitor chat, erp-catalog, asaas webhook without auth"
  artifacts:
    - path: "server/routes/provider.routes.ts"
      provides: "/api/provider/* (profile, settings, users, partners, documents, webhook, n8n, notifications, trial)"
      exports: ["registerProviderRoutes"]
    - path: "server/routes/erp.routes.ts"
      provides: "/api/provider/erp-*, /api/webhooks/erp-*"
      exports: ["registerErpRoutes"]
    - path: "server/routes/admin.routes.ts"
      provides: "/api/admin/* (providers, users, chat, erp-catalog, stats, plan-history)"
      exports: ["registerAdminRoutes"]
    - path: "server/routes/financeiro.routes.ts"
      provides: "/api/admin/invoices/*, /api/admin/financial/*, /api/admin/asaas/*"
      exports: ["registerFinanceiroRoutes"]
    - path: "server/routes/credits.routes.ts"
      provides: "/api/credits/*, /api/admin/credit-orders/*"
      exports: ["registerCreditsRoutes"]
    - path: "server/routes/chat.routes.ts"
      provides: "/api/chat/*, /api/admin/chat/*"
      exports: ["registerChatRoutes"]
    - path: "server/routes/ai.routes.ts"
      provides: "/api/ai/*"
      exports: ["registerAiRoutes"]
    - path: "server/routes/public.routes.ts"
      provides: "/api/public/*, /api/asaas/webhook, /api/erp-catalog, /api/tenant/resolve"
      exports: ["registerPublicRoutes"]
  key_links:
    - from: "server/routes/erp.routes.ts"
      to: "server/routes/utils.ts"
      via: "testErpConnection, fetchErpCustomers helpers"
      pattern: "testErpConnection|fetchErpCustomers"
    - from: "server/routes/admin.routes.ts"
      to: "server/routes/utils.ts"
      via: "testErpConnection helper for admin ERP test"
      pattern: "testErpConnection"
    - from: "server/routes/financeiro.routes.ts"
      to: "server/asaas.ts"
      via: "Asaas payment integration"
      pattern: "asaas"
---

<objective>
Extract the remaining 8 route modules (provider, erp, admin, financeiro, credits, chat, ai, public) from routes.ts.

Purpose: Complete the route extraction. After this plan plus Plan 02, every endpoint from routes.ts exists in a domain module.
Output: 8 Express Router modules covering all remaining endpoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backend-modularization/03-CONTEXT.md
@.planning/research/ARCHITECTURE.md

@server/routes.ts
@server/auth.ts
@server/asaas.ts
@server/ai-analysis.ts
</context>

<interfaces>
<!-- Same Router pattern as Plan 02 -->
```typescript
import { Router } from "express";
import { requireAuth, requireAdmin, requireSuperAdmin } from "../auth";
import { storage } from "../storage";

export function registerXxxRoutes(): Router {
  const router = Router();
  // router.get/post/patch/delete instead of app.get/post/patch/delete
  return router;
}
```

Helper functions from Plan 02's server/routes/utils.ts:
```typescript
export function getOverdueAmountRange(amount: number): string;
export function getRecommendedActions(score: number, hasUnreturnedEquipment: boolean): string[];
export async function testErpConnection(source: string, apiUrl: string, apiUser: string, apiToken: string): Promise<{ ok: boolean; message: string }>;
export async function fetchErpCustomers(source: string, apiUrl: string, apiUser: string, apiToken: string): Promise<{ ok: boolean; message: string; customers: any[] }>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract provider, erp, chat, ai, and public routes</name>
  <files>
    server/routes/provider.routes.ts,
    server/routes/erp.routes.ts,
    server/routes/chat.routes.ts,
    server/routes/ai.routes.ts,
    server/routes/public.routes.ts
  </files>
  <read_first>
    server/routes.ts (lines 1889-2400 for provider/erp, lines 2816-2900 for admin chat, lines 2864-2900 for provider chat, lines 3494-3640 for ai/public/visitor-chat),
    server/ai-analysis.ts (first 10 lines for exports)
  </read_first>
  <action>
    Read routes.ts carefully for each domain's line ranges. Extract by copying handler bodies EXACTLY.

    **provider.routes.ts** (per D-01) — lines ~1889-2340:
    - GET /api/tenant/resolve (line 1889)
    - GET /api/provider/users (line 1903)
    - POST /api/provider/users (line 1916)
    - DELETE /api/provider/users/:id (line 1941)
    - PATCH /api/provider/settings (line 1961)
    - GET /api/provider/profile (line 1976)
    - GET /api/provider/integration (line 1988)
    - POST /api/provider/integration/regenerate-token (line 1998)
    - GET /api/provider/n8n-config (line 2009)
    - PATCH /api/provider/n8n-config (line 2018)
    - POST /api/provider/n8n-config/test (line 2028)
    - PATCH /api/provider/profile (line 2212)
    - GET /api/provider/partners (line 2234)
    - POST /api/provider/partners (line 2243)
    - PATCH /api/provider/partners/:id (line 2261)
    - DELETE /api/provider/partners/:id (line 2278)
    - GET /api/provider/documents (line 2291)
    - POST /api/provider/documents (line 2301)
    - DELETE /api/provider/documents/:id (line 2324)
    - GET /api/provider/documents/:id/download (line 2337)
    - Plus notification settings and trial status endpoints (search routes.ts)

    **erp.routes.ts** (per D-01) — lines ~2066-2212:
    - GET /api/provider/erp-integrations (line 2066)
    - PATCH /api/provider/erp-integrations/:source (line 2075)
    - POST /api/provider/erp-integrations/:source/test (line 2090)
    - POST /api/provider/erp-integrations/:source/sync (line 2106)
    - GET /api/provider/erp-sync-logs (line 2144)
    - GET /api/provider/erp-integration-stats (line 2158)
    - POST /api/webhooks/erp-sync (line 2167)

    Import testErpConnection and fetchErpCustomers from ./utils (created in Plan 02 Task 1).

    **chat.routes.ts** — provider chat + admin chat + visitor chat:
    - GET /api/chat/thread (line 2864)
    - POST /api/chat/thread/messages (line 2875)
    - GET /api/chat/unread (line 2891)
    - GET /api/admin/chat/threads (line 2816)
    - GET /api/admin/chat/threads/:id/messages (line 2825)
    - POST /api/admin/chat/threads/:id/messages (line 2836)
    - PATCH /api/admin/chat/threads/:id/status (line 2852)
    - POST /api/public/visitor-chat/start (line 3540)
    - GET /api/public/visitor-chat/messages (line 3551)
    - POST /api/public/visitor-chat/messages (line 3565)
    - GET /api/public/visitor-chat/unread (line 3581)
    - GET /api/admin/visitor-chats (line 3594)
    - GET /api/admin/visitor-chats/:id/messages (line 3603)
    - POST /api/admin/visitor-chats/:id/messages (line 3614)
    - PATCH /api/admin/visitor-chats/:id/status (line 3627)

    **ai.routes.ts** (per D-01) — lines ~3494-3540:
    - POST /api/ai/analyze-consultation (line 3494)
    - POST /api/ai/analyze-antifraud (line 3517)

    Imports: streamConsultationAnalysis, streamAntiFraudAnalysis from ../ai-analysis

    **public.routes.ts** (per D-01):
    - GET /api/public/erp-catalog (line 3444)
    - GET /api/erp-catalog (line 3454) — note: this one needs requireAuth
    - GET /api/public/lgpd-info — search routes.ts for this

    Note: /api/tenant/resolve goes in provider.routes.ts (no auth needed but provider-domain).
    Note: /api/asaas/webhook goes in financeiro.routes.ts (payment domain).

    CRITICAL: Copy ALL handler logic verbatim. Change only `app.` to `router.`. Preserve all middleware (requireAuth, requireAdmin, requireSuperAdmin) on correct endpoints. Preserve all providerId filtering (per D-09, D-10).
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>5 route files exist, each exports a register function returning Router, all endpoints mapped, TypeScript compiles</acceptance_criteria>
  <done>provider (~20 endpoints), erp (7 endpoints), chat (15 endpoints), ai (2 endpoints), public (3+ endpoints) extracted with identical handler logic</done>
</task>

<task type="auto">
  <name>Task 2: Extract admin, financeiro, and credits routes</name>
  <files>
    server/routes/admin.routes.ts,
    server/routes/financeiro.routes.ts,
    server/routes/credits.routes.ts
  </files>
  <read_first>
    server/routes.ts (lines 2478-2815 for admin, lines 2902-3196 for financeiro/asaas, lines 3197-3443 for credits),
    server/asaas.ts (first 20 lines for exports)
  </read_first>
  <action>
    **admin.routes.ts** (per D-01) — lines ~2478-2815 (the superadmin management routes):
    - GET /api/admin/stats (line 2478)
    - GET /api/admin/providers (line 2487)
    - POST /api/admin/providers (line 2505)
    - PATCH /api/admin/providers/:id (line 2530)
    - DELETE /api/admin/providers/:id (line 2540 and 2611 — check for duplicates, keep both)
    - POST /api/admin/providers/:id/resend-verification (line 2552)
    - POST /api/admin/providers/:id/plan (line 2571)
    - POST /api/admin/providers/:id/credits (line 2590)
    - GET /api/admin/providers/:id/detail (line 2621)
    - GET /api/admin/providers/:id/integration (line 2673)
    - GET /api/admin/providers/:id/n8n-config (line 2689)
    - PATCH /api/admin/providers/:id/n8n-config (line 2699)
    - POST /api/admin/providers/:id/n8n-config/test (line 2710)
    - PUT /api/admin/providers/:id/erp/:source (line 2739)
    - GET /api/admin/users (line 2760)
    - DELETE /api/admin/users/:id (line 2773)
    - PATCH /api/admin/users/:id/email (line 2783)
    - GET /api/admin/plan-history (line 2805)
    - POST /api/admin/erp-catalog (line 3463)
    - PATCH /api/admin/erp-catalog/:id (line 3474)
    - DELETE /api/admin/erp-catalog/:id (line 3484)

    ALL admin routes use requireSuperAdmin middleware.
    Import testErpConnection from ./utils for the n8n-config/test endpoint.

    **financeiro.routes.ts** (per D-01) — lines ~2902-3196:
    - GET /api/admin/asaas/status (line 2902)
    - POST /api/admin/invoices/:id/asaas/charge (line 2917)
    - POST /api/admin/invoices/:id/asaas/sync (line 2965)
    - DELETE /api/admin/invoices/:id/asaas/charge (line 2994)
    - GET /api/admin/invoices/:id/asaas/pix (line 3014)
    - POST /api/asaas/webhook (line 3028) — NO auth (webhook callback)
    - GET /api/admin/financial/saas-metrics (line 3068)
    - GET /api/admin/financial/summary (line 3077)
    - GET /api/admin/invoices (line 3086)
    - GET /api/admin/invoices/:id (line 3096)
    - POST /api/admin/invoices (line 3106)
    - PATCH /api/admin/invoices/:id/status (line 3134)
    - DELETE /api/admin/invoices/:id (line 3148)
    - POST /api/admin/invoices/generate-monthly (line 3161)

    Imports: Asaas functions from ../asaas

    **credits.routes.ts** (per D-01) — lines ~3197-3443:
    - GET /api/credits/orders (line 3197) — requireAuth (provider)
    - POST /api/credits/purchase (line 3207) — requireAuth (provider)
    - GET /api/credits/orders/:id/asaas/pix (line 3266) — requireAuth (provider)
    - GET /api/admin/credit-orders (line 3282) — requireSuperAdmin
    - POST /api/admin/credit-orders (line 3292) — requireSuperAdmin
    - POST /api/admin/credit-orders/:id/release (line 3359) — requireSuperAdmin
    - PATCH /api/admin/credit-orders/:id (line 3369) — requireSuperAdmin
    - POST /api/admin/credit-orders/:id/asaas/charge (line 3380) — requireSuperAdmin
    - POST /api/admin/credit-orders/:id/asaas/sync (line 3410) — requireSuperAdmin
    - GET /api/admin/credit-orders/:id/asaas/pix (line 3431) — requireSuperAdmin

    Note: credits mixes provider endpoints (requireAuth) and admin endpoints (requireSuperAdmin). Both go in the same file since they share the credits domain.

    CRITICAL: Copy ALL handler logic verbatim. admin.routes.ts will be the second-largest file (~800 lines). Do NOT simplify.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>3 route files exist, admin covers all /api/admin non-financial endpoints, financeiro covers invoices/asaas, credits covers credit orders, TypeScript compiles</acceptance_criteria>
  <done>admin (~21 endpoints), financeiro (~14 endpoints), credits (~10 endpoints) extracted with identical handler logic, all requireSuperAdmin middleware preserved</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. All 8 route files export a function returning Express Router
3. Combined with Plan 02's 7 files, ALL endpoints from routes.ts are covered
4. No endpoint is duplicated across modules
5. All requireSuperAdmin calls on admin endpoints preserved
6. The asaas webhook endpoint (/api/asaas/webhook) has NO auth middleware
</verification>

<success_criteria>
- 8 route modules created under server/routes/
- All admin, financial, provider-management endpoints extracted
- Combined with Plan 02, every endpoint from routes.ts has a new home
- Handler logic is byte-for-byte identical
- TypeScript compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-modularization/03-03-SUMMARY.md`
</output>
