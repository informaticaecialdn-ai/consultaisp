---
phase: 02-foundation-docker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/score-engine.ts
  - server/lgpd-masking.ts
  - server/geocoding.ts
  - server/routes.ts
  - server/heatmap-cache.ts
  - server/logger.ts
  - server/index.ts
  - package.json
  - script/build.ts
autonomous: true
requirements: [MOD-01, DOCK-05]

must_haves:
  truths:
    - "calculateIspScore, getRiskTier, getDecisionReco exist as importable functions in server/score-engine.ts"
    - "LGPD masking functions (maskName, maskCpfCnpj, maskCep) exist in server/lgpd-masking.ts"
    - "Geocoding functions (geocodeCity, geocodeCep) exist in server/geocoding.ts"
    - "routes.ts imports from the new modules instead of defining functions inline"
    - "heatmap-cache.ts imports geocoding from server/geocoding.ts"
    - "All console.log/console.error replaced with pino logger calls"
    - "Application logs are JSON in production and pretty-printed in dev"
  artifacts:
    - path: "server/score-engine.ts"
      provides: "Score calculation, risk tier, decision recommendation"
      exports: ["calculateIspScore", "getRiskTier", "getDecisionReco"]
    - path: "server/lgpd-masking.ts"
      provides: "LGPD data masking utilities"
      exports: ["maskName", "maskCpfCnpj", "maskCep", "maskAddress"]
    - path: "server/geocoding.ts"
      provides: "CEP and city geocoding"
      exports: ["geocodeCity", "geocodeCep"]
    - path: "server/logger.ts"
      provides: "Pino logger instance"
      exports: ["logger"]
  key_links:
    - from: "server/routes.ts"
      to: "server/score-engine.ts"
      via: "import { calculateIspScore, getRiskTier, getDecisionReco }"
      pattern: "import.*from.*score-engine"
    - from: "server/routes.ts"
      to: "server/lgpd-masking.ts"
      via: "import { maskName, maskCpfCnpj, maskCep }"
      pattern: "import.*from.*lgpd-masking"
    - from: "server/heatmap-cache.ts"
      to: "server/geocoding.ts"
      via: "import { geocodeCity, geocodeCep }"
      pattern: "import.*from.*geocoding"
    - from: "server/index.ts"
      to: "server/logger.ts"
      via: "import { logger }"
      pattern: "import.*from.*logger"
---

<objective>
Extract pure business logic into testable modules and replace console.log with pino structured logging.

Purpose: MOD-01 requires score engine, LGPD masking, and geocoding as independent modules. DOCK-05 requires structured JSON logging for Docker log aggregation. Both are foundational -- downstream plans depend on the logger and modules existing.

Output: 4 new files (score-engine.ts, lgpd-masking.ts, geocoding.ts, logger.ts), updated routes.ts/heatmap-cache.ts/index.ts with new imports, pino installed.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-docker/02-CONTEXT.md
@.planning/research/STACK.md

@server/routes.ts (lines 1-100 for score engine, lines 830-930 and 1570-1610 for LGPD masking patterns)
@server/heatmap-cache.ts (lines 1-70 for geocoding functions)
@server/index.ts (for current logging and server bootstrap)
@server/db.ts (for pool export needed by shutdown)
@script/build.ts (for build allowlist — pino must be added)
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract score engine, LGPD masking, and geocoding into independent modules</name>
  <files>server/score-engine.ts, server/lgpd-masking.ts, server/geocoding.ts, server/routes.ts, server/heatmap-cache.ts</files>
  <read_first>server/routes.ts (lines 12-100 for calculateIspScore/getRiskTier/getDecisionReco), server/routes.ts (lines 830-860 and 1570-1600 for masking patterns), server/heatmap-cache.ts (lines 31-67 for geocodeCity/geocodeCep)</read_first>
  <behavior>
    - Test: calculateIspScore({maxDaysOverdue:0, totalOverdueAmount:0, unreturnedEquipmentCount:0, contractAgeDays:365, recentConsultationsCount:0, providersWithDebt:0, clientYears:3, neverLate:true, allEquipmentReturned:true}) returns score >= 100 (capped at 100)
    - Test: calculateIspScore({maxDaysOverdue:120, totalOverdueAmount:500, unreturnedEquipmentCount:2, contractAgeDays:30, recentConsultationsCount:5, providersWithDebt:3, clientYears:0, neverLate:false, allEquipmentReturned:false}) returns score 0 (capped at 0)
    - Test: getRiskTier(85) returns tier "low" with label "BAIXO RISCO"
    - Test: getRiskTier(40) returns tier "high" with label "ALTO RISCO"
    - Test: maskName("Joao Silva", false) returns "Joao ***"
    - Test: maskName("Joao Silva", true) returns "Joao Silva" (same provider = no mask)
    - Test: maskCpfCnpj("12345678901", false) returns "123.***.***-**"
    - Test: maskCpfCnpj("12345678000190", false) returns "12.***.***\/1234-**"  (CNPJ)
    - Test: maskCep("01310100", false) returns "01310-***"
    - Test: maskCep("01310100", true) returns "01310-100" (same provider = full)
  </behavior>
  <action>
    **server/score-engine.ts** (per D-01):
    - Extract the `calculateIspScore` function from routes.ts lines 12-87, keeping exact same parameter types and return type.
    - Extract `getRiskTier` (line 89-93) and `getDecisionReco` (line 96-99).
    - Export all three as named exports. The function must be a pure function with zero imports (only uses its params).
    - Type the params interface explicitly: `ScoreInput` and return type `ScoreResult`.

    **server/lgpd-masking.ts** (per D-02):
    - Create `maskName(fullName: string, isSameProvider: boolean): string` — if isSameProvider, return full name. Otherwise, keep first word + " ***" (matching the pattern at routes.ts lines 1582-1585).
    - Create `maskCpfCnpj(cpfCnpj: string, isSameProvider: boolean): string` — if isSameProvider, return original. Otherwise, for CPF (11 digits): `XXX.***.***-**`, for CNPJ (14 digits): `XX.***.***\/XXXX-**` (matching routes.ts lines 1587-1592).
    - Create `maskCep(rawCep: string, isSameProvider: boolean): string` — if isSameProvider, format as `XXXXX-XXX`. Otherwise, `XXXXX-***` (matching routes.ts line 925-930).
    - Create `maskAddress(address: string, isSameProvider: boolean): string` — if isSameProvider, return full. Otherwise, remove house number (strip digits after comma or "n" patterns).
    - Export all four functions.

    **server/geocoding.ts** (per D-03):
    - Move `geocodeCity` (heatmap-cache.ts lines 31-52) and `geocodeCep` (lines 54-67) into this file.
    - Keep the in-memory cache Map (`_cityGeo`) as a module-level variable in geocoding.ts.
    - Export both functions as named exports.

    **Update server/routes.ts** (per D-05):
    - Remove lines 12-99 (calculateIspScore, getRiskTier, getDecisionReco definitions).
    - Add `import { calculateIspScore, getRiskTier, getDecisionReco } from "./score-engine";` at top.
    - DO NOT refactor the inline masking in routes.ts yet — that would be too large a change. Just extract the functions and make them available. The inline masking in routes.ts will be refactored to use lgpd-masking.ts in Phase 3 when routes are modularized.

    **Update server/heatmap-cache.ts**:
    - Remove geocodeCity and geocodeCep function definitions (lines 31-67).
    - Remove the `_cityGeo` cache Map (line 27).
    - Add `import { geocodeCity, geocodeCep } from "./geocoding";` at top.
    - Verify all references to geocodeCity/geocodeCep still work (they're called at lines 266 and 277).
  </action>
  <verify>
    <automated>npx tsx --test server/score-engine.test.ts && npx tsx --test server/lgpd-masking.test.ts && npm run check</automated>
  </verify>
  <acceptance_criteria>
    - server/score-engine.ts exists and exports calculateIspScore, getRiskTier, getDecisionReco
    - server/lgpd-masking.ts exists and exports maskName, maskCpfCnpj, maskCep, maskAddress
    - server/geocoding.ts exists and exports geocodeCity, geocodeCep
    - routes.ts no longer contains function definitions for calculateIspScore/getRiskTier/getDecisionReco (grep returns 0 matches for "function calculateIspScore" in routes.ts)
    - heatmap-cache.ts no longer contains geocodeCity/geocodeCep definitions (grep returns 0 matches)
    - `npm run check` (tsc) passes with zero errors
    - Test files pass all assertions
  </acceptance_criteria>
  <done>Score engine, LGPD masking, and geocoding are independent importable modules. routes.ts and heatmap-cache.ts use the new imports. TypeScript compilation succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Install pino and replace all console.log with structured logging</name>
  <files>server/logger.ts, server/index.ts, server/routes.ts, server/heatmap-cache.ts, server/scheduler.ts, package.json, script/build.ts</files>
  <read_first>server/index.ts (full file), package.json (for current deps), script/build.ts (for allowlist)</read_first>
  <action>
    **Install pino** (per D-12, D-13, D-14):
    ```
    npm install pino@^10 pino-http@^10
    npm install -D pino-pretty@^13
    ```

    **server/logger.ts** — Create centralized logger:
    ```typescript
    import pino from "pino";

    const isProduction = process.env.NODE_ENV === "production";

    export const logger = pino({
      level: process.env.LOG_LEVEL || (isProduction ? "info" : "debug"),
      ...(isProduction
        ? {}  // JSON output in production
        : { transport: { target: "pino-pretty", options: { colorize: true, translateTime: "HH:MM:ss" } } }
      ),
    });
    ```

    **Update server/index.ts** (per D-13):
    - Replace the custom `log()` function (lines 30-38) with `import { logger } from "./logger";`
    - Replace the custom request logging middleware (lines 40-65) with pino-http:
      ```typescript
      import pinoHttp from "pino-http";
      app.use(pinoHttp({ logger, autoLogging: { ignore: (req) => !req.url?.startsWith("/api") } }));
      ```
    - Remove the old `export function log(...)` — search for any imports of `log` from `./index` in other files and replace with logger.
    - Replace `console.log(\`serving on port \${port}\`)` with `logger.info({ port }, "server started")`.
    - Replace `console.error("Internal Server Error:", err)` with `logger.error({ err }, "internal server error")`.

    **Update server/routes.ts**:
    - Add `import { logger } from "./logger";` at top.
    - Replace all `console.log(...)` and `console.warn(...)` and `console.error(...)` calls with `logger.info(...)`, `logger.warn(...)`, `logger.error(...)` respectively. There are ~19 occurrences.
    - For structured logging, extract relevant context: e.g., `console.warn("[ISP-N8N] Address cross-ref failed:", err.message)` becomes `logger.warn({ err: err.message }, "ISP-N8N address cross-ref failed")`.

    **Update server/heatmap-cache.ts**:
    - Add `import { logger } from "./logger";`
    - Replace ~5 console.log/console.error calls with logger equivalents.

    **Update server/scheduler.ts**:
    - Add `import { logger } from "./logger";`
    - Replace ~8 console.log/console.error calls with logger equivalents.

    **Update script/build.ts** allowlist:
    - Add `"pino"`, `"pino-http"` to the allowlist array so esbuild bundles them.

    **Check for `log` import from index.ts**:
    - Search all files for `import { log } from "./index"` or similar and replace with logger import.
    - The `log` function in index.ts was exported — grep for its usage and update all call sites.
  </action>
  <verify>
    <automated>npm run check && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "console\.log\|console\.error\|console\.warn" server/routes.ts server/index.ts server/heatmap-cache.ts server/scheduler.ts` returns 0 matches (all replaced)
    - server/logger.ts exists and exports `logger`
    - package.json contains pino, pino-http as dependencies and pino-pretty as devDependency
    - script/build.ts allowlist includes "pino" and "pino-http"
    - `npm run check` passes (TypeScript compiles)
    - `npm run build` succeeds (production build)
  </acceptance_criteria>
  <done>All server-side logging uses pino structured JSON. No console.log remains in server files. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm run check` passes (TypeScript compiles with no errors)
- `npm run build` succeeds (production build with esbuild + vite)
- `grep -rn "function calculateIspScore" server/routes.ts` returns nothing (extracted)
- `grep -rn "function geocodeCity" server/heatmap-cache.ts` returns nothing (extracted)
- `grep -rn "console\.log" server/routes.ts server/index.ts server/heatmap-cache.ts server/scheduler.ts` returns nothing (replaced)
- server/score-engine.ts, server/lgpd-masking.ts, server/geocoding.ts, server/logger.ts all exist
</verification>

<success_criteria>
1. Score engine (calculateIspScore), LGPD masking (maskName/maskCpfCnpj/maskCep/maskAddress), and geocoding (geocodeCity/geocodeCep) exist as independent modules with their own imports
2. All server-side logging uses pino (structured JSON in production, pretty in dev)
3. Application builds and TypeScript compiles successfully
4. Existing behavior unchanged -- routes.ts still produces same results via imports
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundation-docker/02-01-SUMMARY.md`
</output>
