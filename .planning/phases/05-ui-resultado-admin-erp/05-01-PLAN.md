---
phase: 05-ui-resultado-admin-erp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/public/n8n-ixc-inadimplentes-workflow.json
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04, ADM-01, ADM-02]

must_haves:
  truths:
    - "Score gauge 0-1000 with color bands displayed in consultation result"
    - "Provider details with LGPD masking for cross-provider data"
    - "Condicoes Obrigatorias section based on score"
    - "Analisar com IA button with streaming SSE"
    - "Admin ERP page with dynamic fields per ERP type, no N8N mentions"
    - "Test connection button in admin ERP panel"
---

<objective>
Verify all UI requirements are already implemented and remove last N8N artifact.

All 6 requirements were found to be already implemented in the codebase:
- UI-01: ScoreGaugeSvg + ScoreBreakdown in consulta-isp.tsx
- UI-02: providerDetails with maskCrossProviderDetail
- UI-03: recommendedActions section ("Condicoes Obrigatorias")
- UI-04: POST /api/ai/analyze-consultation with SSE streaming
- ADM-01: IntegracaoTab in admin-provedor.tsx with ERP list
- ADM-02: "Testar Conexao" in admin-sistema.tsx and painel-provedor.tsx

Only cleanup needed: remove obsolete N8N workflow JSON from client/public/.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Remove N8N artifact and verify requirements</name>
  <files>client/public/n8n-ixc-inadimplentes-workflow.json</files>
  <read_first>
    - client/src/pages/consulta-isp.tsx (verify score gauge, breakdown, conditions, AI button)
    - client/src/pages/admin-provedor.tsx (verify ERP integration tab)
    - client/src/pages/admin-sistema.tsx (verify test connection)
  </read_first>
  <action>Delete client/public/n8n-ixc-inadimplentes-workflow.json</action>
  <acceptance_criteria>
    - grep -rq "ScoreGaugeSvg" client/src/pages/consulta-isp.tsx
    - grep -rq "Condicoes Obrigatorias" client/src/pages/consulta-isp.tsx
    - grep -rq "Analisar com IA" client/src/pages/consulta-isp.tsx
    - grep -rq "IntegracaoTab\|Integracao ERP" client/src/pages/admin-provedor.tsx
    - grep -rq "erp-test\|Testar Conexao" client/src/pages/admin-sistema.tsx
    - ! test -f client/public/n8n-ixc-inadimplentes-workflow.json
  </acceptance_criteria>
  <done>All UI requirements verified as already implemented. N8N artifact removed.</done>
</task>
</tasks>
