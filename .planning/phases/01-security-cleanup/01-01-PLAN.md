---
phase: 01-security-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/heatmap-cache.ts
  - server/routes.ts
  - server/email.ts
  - client/src/pages/admin-provedor.tsx
  - vite.config.ts
  - package.json
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03]

must_haves:
  truths:
    - "No hardcoded N8N URLs or credentials exist in source code"
    - "No @replit packages remain in package.json"
    - "No Replit plugin imports remain in vite.config.ts"
    - "No Replit-specific directories exist in the project tree"
    - "server/email.ts derives APP_URL without Replit env vars"
  artifacts:
    - path: "server/heatmap-cache.ts"
      provides: "N8N URL and auth read from process.env"
      contains: "process.env.N8N_PROXY_URL"
    - path: "server/routes.ts"
      provides: "Central N8N URL and auth read from process.env"
      contains: "process.env.CENTRAL_N8N_URL"
    - path: "vite.config.ts"
      provides: "Clean Vite config without Replit plugins"
    - path: "package.json"
      provides: "No @replit dependencies"
  key_links:
    - from: "server/heatmap-cache.ts"
      to: "process.env"
      via: "N8N_PROXY_URL and N8N_PROXY_AUTH env vars"
      pattern: "process\\.env\\.N8N_PROXY"
    - from: "server/routes.ts"
      to: "process.env"
      via: "CENTRAL_N8N_URL and CENTRAL_N8N_AUTH env vars"
      pattern: "process\\.env\\.CENTRAL_N8N"
---

<objective>
Remove all hardcoded secrets from source code and clean all Replit platform artifacts.

Purpose: Eliminate security vulnerabilities (hardcoded credentials) and platform lock-in (Replit artifacts) so the codebase is portable and secret-free.
Output: Source files with env var references instead of hardcoded secrets; no Replit directories, packages, or plugin references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-cleanup/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded N8N secrets with environment variables (SEC-01)</name>
  <files>server/heatmap-cache.ts, server/routes.ts</files>
  <read_first>server/heatmap-cache.ts, server/routes.ts</read_first>
  <action>
In server/heatmap-cache.ts (lines 3-4), replace:
```
const N8N_PROXY_URL = "https://n8n.aluisiocunha.com.br/webhook/ixc-inadimplentes";
const N8N_PROXY_AUTH = "Basic aXNwX2FuYWxpenplOmlzcGFuYWxpenplMTIzMTIz";
```
With:
```
const N8N_PROXY_URL = process.env.N8N_PROXY_URL || "";
const N8N_PROXY_AUTH = process.env.N8N_PROXY_AUTH || "";
```

In server/routes.ts (lines 739-740), replace:
```
const CENTRAL_N8N_URL = "https://n8n.aluisiocunha.com.br/webhook/isp-consult";
const CENTRAL_N8N_AUTH = "Basic aXNwX2FuYWxpenplOmlzcGFuYWxpenplMTIzMTIz";
```
With:
```
const CENTRAL_N8N_URL = process.env.CENTRAL_N8N_URL || "";
const CENTRAL_N8N_AUTH = process.env.CENTRAL_N8N_AUTH || "";
```

Per D-01 and D-02: These env vars are an interim security fix. The N8N dependency itself is removed in Phase 5.
Per D-03: Scan the entire server/ directory for any other hardcoded URLs with credentials, API keys, or tokens beyond the N8N ones. Fix any found using the same pattern (process.env.VARNAME).
  </action>
  <verify>
    <automated>grep -rn "n8n.aluisiocunha\|Basic aXNw" server/ && echo "FAIL: hardcoded secrets found" || echo "PASS: no hardcoded secrets"</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "n8n.aluisiocunha" server/ returns zero matches
    - grep -rn "Basic aXNw" server/ returns zero matches
    - grep -n "process.env.N8N_PROXY_URL" server/heatmap-cache.ts returns a match
    - grep -n "process.env.N8N_PROXY_AUTH" server/heatmap-cache.ts returns a match
    - grep -n "process.env.CENTRAL_N8N_URL" server/routes.ts returns a match
    - grep -n "process.env.CENTRAL_N8N_AUTH" server/routes.ts returns a match
  </acceptance_criteria>
  <done>All N8N URLs and credentials read from environment variables, zero hardcoded secrets in server/</done>
</task>

<task type="auto">
  <name>Task 2: Remove all Replit artifacts, packages, and references (SEC-02, SEC-03)</name>
  <files>vite.config.ts, package.json, server/email.ts, client/src/pages/admin-provedor.tsx</files>
  <read_first>vite.config.ts, package.json, server/email.ts, client/src/pages/admin-provedor.tsx</read_first>
  <action>
**Per D-04: Delete Replit directories (use rm -rf):**
- client/replit_integrations/ (contains: audio)
- server/replit_integrations/ (contains: audio, batch, chat, image)
- .local/ (Replit Agent skills and artifacts)
- .config/replit/ (semgrep rules)
- .replit (Replit config file)
- replit.md (Replit documentation)
- attached_assets/ (development prompts, pasted assets)

Also remove .config/ directory if it becomes empty after removing .config/replit/.

**Per D-05: Remove 3 @replit packages from package.json devDependencies:**
- "@replit/vite-plugin-cartographer": "^0.4.4"
- "@replit/vite-plugin-dev-banner": "^0.1.1"
- "@replit/vite-plugin-runtime-error-modal": "^0.0.3"

Run `npm install` after removing to update package-lock.json.

**Per D-06: Clean vite.config.ts:**
Remove the import: `import runtimeErrorOverlay from "@replit/vite-plugin-runtime-error-modal";`
Remove `runtimeErrorOverlay()` from plugins array.
Remove the entire conditional block that dynamically imports cartographer and dev-banner (lines 10-19 checking REPL_ID).
Remove the `@assets` alias pointing to `attached_assets/` (that directory is being deleted).
The resulting vite.config.ts should have only `react()` in the plugins array, and only `@` and `@shared` aliases.

**Per D-07: Clean Replit references in other source files:**
- server/email.ts (lines 9-10): Remove the two Replit fallback lines:
  ```
  if (process.env.REPLIT_DEPLOYMENT_URL) return `https://${process.env.REPLIT_DEPLOYMENT_URL}`;
  if (process.env.REPLIT_DEV_DOMAIN) return `https://${process.env.REPLIT_DEV_DOMAIN}`;
  ```
  Keep the APP_URL env var check and localhost fallback.

- client/src/pages/admin-provedor.tsx (lines 479, 483): Replace the hardcoded `consultaisp-isp.replit.app` DNS CNAME destination with a placeholder or environment-driven value. Change to a generic comment or use the app's actual domain. Replace with `app.consultaisp.com.br` as the CNAME target (this is the production domain direction). If the exact domain is unclear, use `[SEU-DOMINIO]` as placeholder text.
  </action>
  <verify>
    <automated>grep -rn "@replit" package.json && echo "FAIL" || echo "PASS: no replit packages"; grep -rn "replit" vite.config.ts && echo "FAIL" || echo "PASS: clean vite config"; ls -d client/replit_integrations server/replit_integrations .local .config/replit .replit replit.md attached_assets 2>/dev/null && echo "FAIL: directories still exist" || echo "PASS: directories removed"</automated>
  </verify>
  <acceptance_criteria>
    - ls client/replit_integrations 2>/dev/null returns error (directory gone)
    - ls server/replit_integrations 2>/dev/null returns error (directory gone)
    - ls .local 2>/dev/null returns error (directory gone)
    - ls .config/replit 2>/dev/null returns error (directory gone)
    - ls .replit 2>/dev/null returns error (file gone)
    - ls replit.md 2>/dev/null returns error (file gone)
    - ls attached_assets 2>/dev/null returns error (directory gone)
    - grep "@replit" package.json returns zero matches
    - grep -i "replit" vite.config.ts returns zero matches
    - grep "REPLIT_DEPLOYMENT_URL\|REPLIT_DEV_DOMAIN" server/email.ts returns zero matches
    - grep "replit.app" client/src/pages/admin-provedor.tsx returns zero matches
    - vite.config.ts contains only react() in the plugins array (no runtimeErrorOverlay, no cartographer, no devBanner)
  </acceptance_criteria>
  <done>All Replit directories deleted, all @replit packages removed from package.json, vite.config.ts clean of Replit plugins, no Replit references in source code</done>
</task>

</tasks>

<verification>
- grep -rn "n8n.aluisiocunha\|Basic aXNw" server/ returns zero matches
- grep -rn "@replit" package.json returns zero matches
- grep -rn "replit" vite.config.ts returns zero matches
- No Replit directories exist (client/replit_integrations, server/replit_integrations, .local, .config/replit, .replit, replit.md, attached_assets)
</verification>

<success_criteria>
1. All hardcoded N8N credentials replaced with process.env references
2. All 7 Replit directories/files deleted
3. All 3 @replit packages removed from package.json
4. vite.config.ts contains zero Replit plugin references
5. server/email.ts has no Replit env var fallbacks
6. admin-provedor.tsx has no replit.app domain reference
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-cleanup/01-01-SUMMARY.md`
</output>
