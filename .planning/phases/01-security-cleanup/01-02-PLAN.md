---
phase: 01-security-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - shared/schema.ts
  - client/src/pages/admin-sistema.tsx
  - client/src/pages/admin-financeiro.tsx
  - client/src/pages/invoice-view.tsx
  - server/storage.ts
  - server/routes.ts
autonomous: true
requirements: [FIX-01]

must_haves:
  truths:
    - "Landing page prices and PLAN_PRICES constant show the same values"
    - "All duplicate PLAN_PRICES definitions use the canonical values from shared/schema.ts"
    - "The application builds with zero TypeScript errors after all Phase 1 changes"
    - "The application runs (npm run dev starts without crash)"
  artifacts:
    - path: "shared/schema.ts"
      provides: "Canonical PLAN_PRICES constant"
      contains: "basic: 149"
    - path: "client/src/pages/admin-financeiro.tsx"
      provides: "Invoice form uses imported PLAN_PRICES"
      contains: "PLAN_PRICES"
    - path: "server/storage.ts"
      provides: "MRR calculation uses imported PLAN_PRICES"
      contains: "PLAN_PRICES"
  key_links:
    - from: "client/src/pages/admin-financeiro.tsx"
      to: "shared/schema.ts"
      via: "import PLAN_PRICES"
      pattern: "import.*PLAN_PRICES.*from.*schema"
    - from: "server/storage.ts"
      to: "shared/schema.ts"
      via: "import PLAN_PRICES"
      pattern: "import.*PLAN_PRICES.*from.*schema"
    - from: "server/routes.ts"
      to: "shared/schema.ts"
      via: "import PLAN_PRICES"
      pattern: "import.*PLAN_PRICES.*from.*schema"
---

<objective>
Unify divergent price constants across the codebase and validate the full build after all Phase 1 changes.

Purpose: Landing page shows 0/149/349 but PLAN_PRICES says 0/199/399/799 -- billing logic could charge wrong amounts. Also validates that Plan 01's removals didn't break the build.
Output: Single source of truth for plan prices; clean build (tsc + vite).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-security-cleanup/01-CONTEXT.md
@.planning/phases/01-security-cleanup/01-01-SUMMARY.md

<interfaces>
From shared/schema.ts (current - to be changed):
```typescript
export const PLAN_PRICES: Record<string, number> = {
  free: 0,
  basic: 199,
  pro: 399,
  enterprise: 799,
};
```

Duplicated in (all must be updated or replaced with import):
- client/src/pages/invoice-view.tsx:22 (local const, values: free:0, basic:199, pro:399, enterprise:799)
- client/src/pages/admin-sistema.tsx:2552 and :2586 (inline const, same old values)
- client/src/pages/admin-financeiro.tsx:21 (local const, same old values)
- server/storage.ts:889 and :1313 (local const, same old values)
- server/routes.ts:3587 (local const, same old values)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Unify PLAN_PRICES to match landing page and eliminate duplicates (FIX-01)</name>
  <files>shared/schema.ts, client/src/pages/invoice-view.tsx, client/src/pages/admin-sistema.tsx, client/src/pages/admin-financeiro.tsx, server/storage.ts, server/routes.ts</files>
  <read_first>shared/schema.ts, client/src/pages/landingpage.tsx, client/src/pages/invoice-view.tsx, client/src/pages/admin-sistema.tsx, client/src/pages/admin-financeiro.tsx, server/storage.ts, server/routes.ts</read_first>
  <action>
Per D-08 (Claude's discretion): The landing page prices (0/149/349) are the current commercial offering shown to customers. Update PLAN_PRICES in shared/schema.ts to match:

```typescript
export const PLAN_PRICES: Record<string, number> = {
  free: 0,
  basic: 149,
  pro: 349,
  enterprise: 799,
};
```

Note: enterprise stays at 799 since the landing page only shows 3 tiers (Gratuito/Basico/Profissional). Enterprise is not displayed on landing page so 799 is kept as-is.

Then eliminate ALL duplicate PLAN_PRICES definitions across the codebase by importing from the canonical source:

1. **client/src/pages/invoice-view.tsx** (line 22): Remove the local `const PLAN_PRICES` and add `import { PLAN_PRICES } from "@shared/schema";` at the top.

2. **client/src/pages/admin-sistema.tsx** (lines 2552 and 2586): Remove both inline `const PLAN_PRICES` definitions. Add `import { PLAN_PRICES } from "@shared/schema";` at the top (if not already imported -- this file likely already imports from schema).

3. **client/src/pages/admin-financeiro.tsx** (line 21): Remove the local `const PLAN_PRICES` and add `import { PLAN_PRICES } from "@shared/schema";` (or add to existing schema import).

4. **server/storage.ts** (lines 889 and 1313): Remove both local `const PLAN_PRICES` definitions. Add `import { PLAN_PRICES } from "@shared/schema";` at the top (or use the relative path `../shared/schema` -- check existing import pattern in this file).

5. **server/routes.ts** (line 3587): Remove the local `const PLAN_PRICES` definition. Import from shared/schema using the same pattern as other imports in this file.

For each file, verify the import path convention:
- Client files use: `@shared/schema` (via Vite alias)
- Server files use: `../shared/schema` or check existing imports in the file for the correct relative path pattern
  </action>
  <verify>
    <automated>grep -rn "PLAN_PRICES" shared/schema.ts server/storage.ts server/routes.ts client/src/pages/invoice-view.tsx client/src/pages/admin-sistema.tsx client/src/pages/admin-financeiro.tsx | grep -v "import" | grep -v "shared/schema.ts" && echo "FAIL: duplicate definitions remain" || echo "PASS: single source of truth"</automated>
  </verify>
  <acceptance_criteria>
    - grep "basic: 149" shared/schema.ts returns a match
    - grep "pro: 349" shared/schema.ts returns a match
    - grep "enterprise: 799" shared/schema.ts returns a match
    - grep -c "const PLAN_PRICES" client/src/pages/invoice-view.tsx returns 0
    - grep -c "const PLAN_PRICES" client/src/pages/admin-financeiro.tsx returns 0
    - grep "PLAN_PRICES" client/src/pages/admin-financeiro.tsx returns only an import line
    - grep -c "const PLAN_PRICES" server/storage.ts returns 0
    - grep -c "const PLAN_PRICES" server/routes.ts returns 0 (for the local definition, not usages)
    - grep "import.*PLAN_PRICES" server/storage.ts returns a match
    - grep "import.*PLAN_PRICES" server/routes.ts returns a match
  </acceptance_criteria>
  <done>PLAN_PRICES defined once in shared/schema.ts with values 0/149/349/799, all other files import from there</done>
</task>

<task type="auto">
  <name>Task 2: Build validation after all Phase 1 changes (D-09)</name>
  <files></files>
  <read_first>package.json</read_first>
  <action>
Per D-09: Run the full build pipeline to confirm nothing is broken after secrets removal, Replit cleanup, and price unification.

1. Run `npm run check` (tsc -- TypeScript type checking). Must exit with code 0 and zero errors.
2. Run `npm run build` (tsx script/build.ts -- full production build). Must exit with code 0.

If either fails, diagnose and fix the issue. Common expected issues:
- Missing imports after removing Replit packages (fix: remove the dead import)
- TypeScript errors from changed PLAN_PRICES imports (fix: correct the import path)
- Build errors from removed attached_assets alias (fix: already handled in Plan 01, but verify no code references @assets)

Do NOT skip errors or suppress warnings that indicate real problems.
  </action>
  <verify>
    <automated>npm run check 2>&1 | tail -5 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - npm run check exits with code 0 (echo $? returns 0)
    - npm run build exits with code 0 (echo $? returns 0)
    - No TypeScript errors in output of npm run check
    - dist/ directory exists with built files after npm run build
  </acceptance_criteria>
  <done>TypeScript check passes with zero errors, production build succeeds, dist/ contains output files</done>
</task>

</tasks>

<verification>
- shared/schema.ts PLAN_PRICES: free=0, basic=149, pro=349, enterprise=799
- Zero duplicate PLAN_PRICES const definitions outside shared/schema.ts
- npm run check exits 0
- npm run build exits 0
</verification>

<success_criteria>
1. PLAN_PRICES canonical values match landing page (0/149/349) with enterprise at 799
2. All 6 duplicate PLAN_PRICES definitions replaced with imports from shared/schema
3. TypeScript check passes (npm run check = 0 errors)
4. Production build succeeds (npm run build = exit 0)
</success_criteria>

<output>
After completion, create `.planning/phases/01-security-cleanup/01-02-SUMMARY.md`
</output>
