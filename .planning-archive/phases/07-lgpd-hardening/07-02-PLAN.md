---
phase: 07-lgpd-hardening
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - server/routes/consultas.routes.ts
autonomous: true
requirements:
  - LGPD-02
  - LGPD-03

must_haves:
  truths:
    - "ISP consultation ERP-central path uses maskCrossProviderDetail for all providerDetails entries"
    - "ISP consultation local-DB path uses maskCrossProviderDetail for all providerDetails entries"
    - "Address cross-reference matches use maskCrossProviderDetail"
    - "CEP fallback history results use maskCrossProviderDetail"
    - "No inline masking logic remains in consultas.routes.ts — all masking delegated to lgpd-masking.ts"
    - "Same-provider data still shows full details (name, CPF, address, exact amounts)"
    - "Cross-provider data shows only: partial name, masked CPF, street without number, masked CEP, value range"
  artifacts:
    - path: "server/routes/consultas.routes.ts"
      provides: "ISP consultation endpoints with centralized LGPD masking"
      contains: "maskCrossProviderDetail"
  key_links:
    - from: "server/routes/consultas.routes.ts"
      to: "server/lgpd-masking.ts"
      via: "import { maskCrossProviderDetail, maskName, maskCpfCnpj }"
      pattern: "import.*lgpd-masking"
    - from: "server/routes/consultas.routes.ts providerDetails.map"
      to: "maskCrossProviderDetail"
      via: "function call on each detail object"
      pattern: "maskCrossProviderDetail\\("
---

<objective>
Replace all inline ad-hoc masking in consultas.routes.ts with calls to the centralized maskCrossProviderDetail function from lgpd-masking.ts.

Purpose: Ensure no API endpoint returns complete personal data of customers belonging to a different provider (per D-04, D-05, D-06). Eliminate duplicated masking logic and fix the local-DB path bug where customer.name is returned unmasked for cross-provider records.
Output: consultas.routes.ts uses centralized masking with zero inline masking logic.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-lgpd-hardening/07-01-SUMMARY.md

@server/lgpd-masking.ts
@server/routes/consultas.routes.ts
</context>

<interfaces>
<!-- From Plan 01: centralized masking functions -->
```typescript
// server/lgpd-masking.ts
export function maskName(fullName: string, isSameProvider: boolean): string;
export function maskCpfCnpj(cpfCnpj: string, isSameProvider: boolean): string;
export function maskCep(rawCep: string, isSameProvider: boolean): string;
export function maskAddress(address: string, isSameProvider: boolean): string;
export function maskOverdueAmount(amount: number, isSameProvider: boolean): number | undefined;
export function getOverdueAmountRange(amount: number): string;
export function maskCrossProviderDetail(detail: Record<string, any>, isSameProvider: boolean): Record<string, any>;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Refactor ERP-central path to use maskCrossProviderDetail</name>
  <read_first>server/routes/consultas.routes.ts (lines 1-510), server/lgpd-masking.ts</read_first>
  <files>server/routes/consultas.routes.ts</files>
  <action>
    1. Add import at top: `import { maskCrossProviderDetail, maskName } from '../lgpd-masking';`

    2. **ERP-central providerDetails mapping (~line 255-375):** The current code builds each detail object with inline masking (ternaries on `isSame` for customerName, address, cep, overdueAmount, overdueAmountRange, phone, email, etc.). Refactor to:
      a) Build the detail object with ALL fields as if same-provider (full data).
      b) After building, apply: `return maskCrossProviderDetail(detail, isSame);`
      This replaces ALL the inline `isSame ? fullValue : maskedValue` ternaries.

    Specifically, change the providerDetails.map lambda (~line 255) to:
    - Set `customerName` to the raw full_name (not the inline IIFE that masks)
    - Set `address` to `addrFull` always (not the isSame ternary)
    - Set `cep` to `fullCep` always (not the isSame ternary)
    - Set `overdueAmount` to the raw overdueAmount number always
    - Remove the inline `overdueAmountRange` calculation
    - Set `phone`, `email`, `planName`, `lastPaymentDate`, `lastPaymentValue`, `openAmountTotal`, `openItems` to their full values
    - Then `return maskCrossProviderDetail(rawDetail, isSame);`

    3. **Address cross-reference matches (~line 200-225):** The erpAddressMatches building has inline masking for acMaskedName and cpfCnpj. Refactor to build raw object, then apply maskCrossProviderDetail:
    - Build match object with full `customerName: acFullName`, full `cpfCnpj: ac.document`
    - Apply maskCrossProviderDetail(match, acIsSame) to each match
    - Keep the extra fields (isFromHistory not in maskCrossProviderDetail — append after masking if needed)

    4. **CEP fallback history results (~line 429-472):** The cepFallbackDetails loop has inline re-masking. Refactor to:
    - Build raw detail from history data
    - Apply maskCrossProviderDetail(detail, hdIsSame)
    - The `isFromHistory: true` flag should be added after masking

    5. **Verify no inline masking remains:** Search for patterns like `parts[0] + " ***"`, `isSame ?` with masking logic, manual CEP truncation. All should be replaced by maskCrossProviderDetail calls.

    IMPORTANT: Do NOT change any business logic (score calculation, credit charging, alert creation, API calls). Only change how the response data is masked before being returned to the client.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - consultas.routes.ts imports maskCrossProviderDetail from lgpd-masking
    - ERP-central providerDetails uses maskCrossProviderDetail on every detail entry
    - Address cross-reference matches use maskCrossProviderDetail
    - CEP fallback history results use maskCrossProviderDetail
    - No inline name/address/CPF/amount masking remains in consultas.routes.ts
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>All cross-provider data masking in consultas.routes.ts is delegated to the centralized lgpd-masking.ts module</done>
</task>

<task type="auto">
  <name>Task 2: Refactor local-DB path and fix unmasked customer.name bug</name>
  <read_first>server/routes/consultas.routes.ts (lines 516-700)</read_first>
  <files>server/routes/consultas.routes.ts</files>
  <action>
    1. **Local-DB providerDetails loop (~line 534-621):** The current code builds detail objects with `customerName: customer.name` (line 590) — this is a BUG: cross-provider customer names are NOT masked. The `overdueAmountRange` uses getOverdueAmountRange from utils but other fields lack masking.

    Refactor to:
    a) Build the raw detail object with full data: `customerName: customer.name`, `overdueAmount: totalOverdue` (always), remove the inline `isSameProvider ? totalOverdue : undefined` pattern.
    b) Include address fields if available from customer record.
    c) After building the full detail, apply: `const maskedDetail = maskCrossProviderDetail(rawDetail, isSameProvider);`
    d) Then add the same-provider-only extras (equipmentDetails, cancelledDate) conditionally:
       ```
       if (isSameProvider) {
         maskedDetail.equipmentDetails = unreturnedEquipment.map(...);
         if (newestContract?.endDate) maskedDetail.cancelledDate = newestContract.endDate;
       } else {
         maskedDetail.equipmentPendingSummary = ...;
       }
       ```
    e) Push maskedDetail to providerDetails.

    2. **Alert creation (~line 655-707):** The alerts store `customer.name` and `customer.cpfCnpj` in the database. This is CORRECT behavior — alerts are shown to the owning provider about their OWN customers. Do NOT mask alert creation data.

    3. Remove the now-unused import of `getOverdueAmountRange` from utils.ts if no longer needed directly (maskCrossProviderDetail handles it internally).

    4. Final audit: grep consultas.routes.ts for any remaining `isSame` or `isSameProvider` ternaries that perform data masking (not business logic). All masking ternaries should be gone, replaced by maskCrossProviderDetail.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Local-DB path cross-provider customer names are now masked (bug fixed)
    - Local-DB path uses maskCrossProviderDetail for all detail objects
    - Alert creation still uses full unmasked customer data (alerts go to owning provider)
    - No remaining inline masking ternaries in consultas.routes.ts for data fields
    - TypeScript compiles without errors
    - The getOverdueAmountRange import is cleaned up or retained only if needed elsewhere
  </acceptance_criteria>
  <done>Both code paths in consultas.routes.ts use centralized masking. No API endpoint returns complete personal data of cross-provider customers. The customer.name bug is fixed.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes — no type errors
- `grep -c "maskCrossProviderDetail" server/routes/consultas.routes.ts` returns 4+ (ERP detail, address match, CEP fallback, local-DB detail)
- `grep -c "parts\[0\].*\*\*\*" server/routes/consultas.routes.ts` returns 0 (no inline name masking)
- `grep -c "isSame.*maskedCep\|isSame.*addrRestricted" server/routes/consultas.routes.ts` returns 0 (no inline address/cep masking)
</verification>

<success_criteria>
- All cross-provider data in ISP consultation responses is masked through lgpd-masking.ts
- Same-provider data shows full details unchanged
- The local-DB path bug (unmasked customer.name) is fixed
- Zero inline masking logic remains in consultas.routes.ts
- Application builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/07-lgpd-hardening/07-02-SUMMARY.md`
</output>
