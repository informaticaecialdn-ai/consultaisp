---
phase: 07-lgpd-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lgpd-masking.ts
  - server/lgpd-masking.test.ts
autonomous: true
requirements:
  - LGPD-01
  - LGPD-02

must_haves:
  truths:
    - "maskCrossProviderDetail() masks name, CPF, address, CEP, and financial data in one call"
    - "Same-provider data passes through unmasked"
    - "Cross-provider name shows only first name + ***"
    - "Cross-provider CPF shows XXX.***.***-**"
    - "Cross-provider address strips house number"
    - "Cross-provider CEP shows only prefix (XXXXX-***)"
    - "Cross-provider overdue amount shows range bracket, not exact value"
  artifacts:
    - path: "server/lgpd-masking.ts"
      provides: "Centralized LGPD masking functions + maskCrossProviderDetail aggregator"
      exports: ["maskName", "maskCpfCnpj", "maskCep", "maskAddress", "maskOverdueAmount", "maskCrossProviderDetail"]
    - path: "server/lgpd-masking.test.ts"
      provides: "Unit tests for all masking functions"
  key_links:
    - from: "server/lgpd-masking.ts"
      to: "server/routes/consultas.routes.ts"
      via: "import { maskCrossProviderDetail } from '../lgpd-masking'"
      pattern: "maskCrossProviderDetail"
---

<objective>
Enhance the centralized LGPD masking module with all required masking functions and a single-call aggregator that masks an entire cross-provider detail object.

Purpose: Create the foundation that Plan 02 will wire into route handlers, ensuring consistent masking logic in one place (per D-02, D-06).
Output: Enhanced server/lgpd-masking.ts with maskOverdueAmount + maskCrossProviderDetail, plus comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@server/lgpd-masking.ts
@server/lgpd-masking.test.ts
@server/routes/utils.ts
</context>

<interfaces>
<!-- Existing masking functions in server/lgpd-masking.ts -->
```typescript
export function maskName(fullName: string, isSameProvider: boolean): string;
export function maskCpfCnpj(cpfCnpj: string, isSameProvider: boolean): string;
export function maskCep(rawCep: string, isSameProvider: boolean): string;
export function maskAddress(address: string, isSameProvider: boolean): string;
```

<!-- From server/routes/utils.ts — existing range function to consolidate -->
```typescript
export function getOverdueAmountRange(amount: number): string {
  if (amount === 0) return "Sem debito";
  if (amount <= 100) return "Ate R$ 100";
  if (amount <= 300) return "R$ 100 - R$ 300";
  if (amount <= 500) return "R$ 300 - R$ 500";
  if (amount <= 1000) return "R$ 500 - R$ 1.000";
  return "Acima de R$ 1.000";
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add maskOverdueAmount and maskCrossProviderDetail to lgpd-masking.ts</name>
  <read_first>server/lgpd-masking.ts, server/lgpd-masking.test.ts, server/routes/utils.ts</read_first>
  <files>server/lgpd-masking.ts, server/lgpd-masking.test.ts</files>
  <behavior>
    - maskOverdueAmount(150, false) returns "R$ 100 - R$ 200" (floor to nearest 100, +100 range)
    - maskOverdueAmount(150, true) returns 150 (passthrough for same provider)
    - maskOverdueAmount(0, false) returns undefined (no debt = nothing to show)
    - maskCrossProviderDetail({ customerName, cpfCnpj, address, cep, overdueAmount, ... }, false) returns object with all fields masked
    - maskCrossProviderDetail(detail, true) returns object with all fields unmasked (passthrough)
    - Cross-provider detail hides: phone, email, planName, lastPaymentDate, lastPaymentValue, openAmountTotal, openItems (set to undefined)
    - Cross-provider detail keeps: providerName, isSameProvider, status, daysOverdue, overdueInvoicesCount, contractAgeDays, hasUnreturnedEquipment
  </behavior>
  <action>
    1. Write tests first in server/lgpd-masking.test.ts covering all behaviors listed above.

    2. Add to server/lgpd-masking.ts:

    a) `maskOverdueAmount(amount: number, isSameProvider: boolean): number | undefined` — per D-03, returns value range for cross-provider. Use the ERP-path pattern from consultas.routes.ts: `Math.floor(amount / 100) * 100` to `+100`. For same-provider, return exact amount. For 0 amount cross-provider, return undefined.

    b) `getOverdueAmountRange(amount: number): string` — move the range-bracket function from server/routes/utils.ts into lgpd-masking.ts (re-export from utils.ts to avoid breaking existing imports). This serves the local-DB code path.

    c) `maskCrossProviderDetail(detail: Record<string, any>, isSameProvider: boolean): Record<string, any>` — single aggregator function that:
      - If isSameProvider is true, returns the detail object unchanged
      - If false, applies maskName to customerName, maskCpfCnpj to cpfCnpj (if present), maskAddress to address, maskCep to cep
      - Sets overdueAmount to undefined, adds overdueAmountRange using the range logic
      - Strips sensitive fields: phone, email, planName, lastPaymentDate, lastPaymentValue, openAmountTotal, openItems
      - Preserves: providerName, isSameProvider, status, daysOverdue, overdueInvoicesCount, contractStartDate, contractAgeDays, hasUnreturnedEquipment, unreturnedEquipmentCount, equipmentPendingSummary, contractStatus

    3. Keep the existing function signatures unchanged (backward compatible). The new functions are additive.

    4. In server/routes/utils.ts, replace the getOverdueAmountRange body with a re-export from lgpd-masking.ts to maintain backward compat: `export { getOverdueAmountRange } from '../lgpd-masking';`
  </action>
  <verify>
    <automated>npx vitest run server/lgpd-masking.test.ts 2>/dev/null || npx tsx --test server/lgpd-masking.test.ts 2>/dev/null || node -e "const m = require('./server/lgpd-masking'); console.log(m.maskName('Joao Silva', false) === 'Joao ***' ? 'PASS' : 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - maskOverdueAmount returns range string for cross-provider, exact number for same-provider
    - maskCrossProviderDetail masks all sensitive fields for cross-provider data
    - maskCrossProviderDetail passes through all fields for same-provider data
    - All existing masking functions (maskName, maskCpfCnpj, maskCep, maskAddress) unchanged
    - getOverdueAmountRange re-exported from utils.ts so existing imports work
  </acceptance_criteria>
  <done>All masking functions exist, tests pass, no existing function signatures broken</done>
</task>

</tasks>

<verification>
- `npx vitest run server/lgpd-masking.test.ts` passes (or equivalent test runner)
- `npx tsc --noEmit` passes (no type errors)
- Existing imports of lgpd-masking functions still resolve
</verification>

<success_criteria>
- server/lgpd-masking.ts exports maskOverdueAmount, getOverdueAmountRange, and maskCrossProviderDetail
- All unit tests pass covering same-provider passthrough and cross-provider masking
- No breaking changes to existing function signatures
</success_criteria>

<output>
After completion, create `.planning/phases/07-lgpd-hardening/07-01-SUMMARY.md`
</output>
