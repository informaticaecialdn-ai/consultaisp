---
phase: 03-backend-modularization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/auth.routes.ts
  - server/routes/dashboard.routes.ts
  - server/routes/import.routes.ts
  - server/routes/consultas.routes.ts
  - server/routes/antifraude.routes.ts
  - server/routes/equipamentos.routes.ts
  - server/routes/heatmap.routes.ts
autonomous: true
requirements: [MOD-02, MOD-04]

must_haves:
  truths:
    - "Auth routes handle login, register, verify-email, logout, me endpoints identically to current behavior"
    - "Dashboard routes return same data for stats, defaulters, inadimplentes, customers, invoices, equipment, contracts"
    - "Import routes handle CSV import for customers, invoices, equipment with same validation"
    - "Consultas routes handle ISP and SPC consultations including batch (lote) with same score calculation"
    - "Anti-fraud routes return alerts and customer risk with same providerId filtering"
    - "Equipamentos routes handle CRUD and import identically"
    - "Heatmap routes return provider, regional, and city-ranking data"
  artifacts:
    - path: "server/routes/auth.routes.ts"
      provides: "/api/auth/* endpoints"
      exports: ["registerAuthRoutes"]
    - path: "server/routes/dashboard.routes.ts"
      provides: "/api/dashboard/*, /api/customers, /api/inadimplentes, /api/defaulters, /api/invoices, /api/equipment, /api/contracts"
      exports: ["registerDashboardRoutes"]
    - path: "server/routes/import.routes.ts"
      provides: "/api/import/* endpoints"
      exports: ["registerImportRoutes"]
    - path: "server/routes/consultas.routes.ts"
      provides: "/api/isp-consultations/*, /api/spc-consultations/*"
      exports: ["registerConsultasRoutes"]
    - path: "server/routes/antifraude.routes.ts"
      provides: "/api/anti-fraud/* endpoints"
      exports: ["registerAntiFraudeRoutes"]
    - path: "server/routes/equipamentos.routes.ts"
      provides: "/api/equipamentos/* endpoints"
      exports: ["registerEquipamentosRoutes"]
    - path: "server/routes/heatmap.routes.ts"
      provides: "/api/heatmap/*, /api/config/maps-key"
      exports: ["registerHeatmapRoutes"]
  key_links:
    - from: "server/routes/*.routes.ts"
      to: "server/storage"
      via: "import { storage } from '../storage'"
      pattern: "storage\\."
    - from: "server/routes/*.routes.ts"
      to: "server/auth.ts"
      via: "import { requireAuth } from '../auth'"
      pattern: "requireAuth"
---

<objective>
Extract 7 route modules (auth, dashboard, import, consultas, antifraude, equipamentos, heatmap) from the monolithic routes.ts.

Purpose: Break out the first half of route domains. These are the simpler/medium-complexity routes that serve provider-facing features.
Output: 7 Express Router modules, each exporting a register function.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-backend-modularization/03-CONTEXT.md
@.planning/research/ARCHITECTURE.md

@server/routes.ts
@server/auth.ts
@server/index.ts
</context>

<interfaces>
<!-- Each route module follows this pattern (per D-01, D-02, D-04): -->
```typescript
import { Router } from "express";
import { requireAuth, requireAdmin, requireSuperAdmin } from "../auth";
import { storage } from "../storage";
// ... other imports as needed

export function registerXxxRoutes(): Router {
  const router = Router();

  router.get("/api/xxx/endpoint", requireAuth, async (req, res) => {
    // Move handler body EXACTLY from routes.ts
  });

  return router;
}
```

Key signature from routes.ts that these modules replace:
```typescript
// Current: export async function registerRoutes(httpServer: Server, app: Express): Promise<Server>
// All app.get/post/patch/delete calls become router.get/post/patch/delete
```

Auth middleware exports from server/auth.ts:
```typescript
export const requireAuth: RequestHandler;
export const requireAdmin: RequestHandler;
export const requireSuperAdmin: RequestHandler;
export const sessionMiddleware: RequestHandler;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract auth, dashboard, import, and equipamentos routes</name>
  <files>
    server/routes/auth.routes.ts,
    server/routes/dashboard.routes.ts,
    server/routes/import.routes.ts,
    server/routes/equipamentos.routes.ts
  </files>
  <read_first>
    server/routes.ts (lines 133-560 for auth/dashboard/import routes, lines 1783-1890 for contracts/antifraude — scan to find equipamentos),
    server/auth.ts (first 30 lines for exports)
  </read_first>
  <action>
    Read routes.ts carefully to identify exact line ranges for each domain. Extract by copying handler bodies EXACTLY.

    **auth.routes.ts** (per D-01) — lines ~139-293:
    - POST /api/auth/login (line 139)
    - GET /api/auth/check-subdomain (line 164)
    - POST /api/auth/register (line 171)
    - GET /api/auth/verify-email (line 226)
    - POST /api/auth/resend-verification (line 250)
    - POST /api/auth/logout (line 277)
    - GET /api/auth/me (line 283)

    Imports needed: storage, loginSchema, registerSchema, PLAN_PRICES from @shared/schema, hashPassword, verifyPassword from ../password, sendVerificationEmail from ../email, slugifySubdomain, buildSubdomainUrl from ../tenant, crypto, logger

    **dashboard.routes.ts** (per D-01) — lines ~295-370:
    - GET /api/dashboard/stats (line 295)
    - GET /api/dashboard/defaulters (line 304)
    - GET /api/inadimplentes (line 313)
    - GET /api/customers (line 322)
    - POST /api/customers (line 331)
    - GET /api/defaulters (line 343)
    - GET /api/invoices (line 352)
    - GET /api/equipment (line 361)
    - GET /api/contracts (line 1880)

    **import.routes.ts** (per D-01) — lines ~372-555:
    - POST /api/import/customers (line 372)
    - POST /api/import/invoices (line 416)
    - POST /api/import/equipment (line 500)

    **equipamentos.routes.ts** — find the /api/equipamentos/* routes in routes.ts (search for "equipamentos"). Includes CRUD + import + status update.

    CRITICAL rules:
    - Change `app.get(` to `router.get(` etc.
    - Change `req.session.providerId` access to remain EXACTLY as-is (multi-tenant per D-09, D-10)
    - Copy ALL try/catch blocks, ALL validation, ALL error responses verbatim
    - Do NOT refactor, simplify, or "improve" any handler logic
    - Include the helper functions used by these routes (getOverdueAmountRange, getRecommendedActions) either inline or in a shared server/routes/utils.ts
    - Move testErpConnection and fetchErpCustomers helpers to server/routes/utils.ts (they're used by erp and admin routes in Plan 03)
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>4 route files exist, each exports a register function returning Router, TypeScript compiles</acceptance_criteria>
  <done>auth (7 endpoints), dashboard (9 endpoints), import (3 endpoints), equipamentos routes extracted with identical handler logic</done>
</task>

<task type="auto">
  <name>Task 2: Extract consultas, antifraude, and heatmap routes</name>
  <files>
    server/routes/consultas.routes.ts,
    server/routes/antifraude.routes.ts,
    server/routes/heatmap.routes.ts
  </files>
  <read_first>
    server/routes.ts (lines 556-1800 for consultas, lines 1783-1880 for antifraude, lines 2406-2477 for heatmap),
    server/score-engine.ts (first 10 lines for exports),
    server/lgpd-masking.ts (first 10 lines for exports),
    server/heatmap-cache.ts (first 10 lines for exports)
  </read_first>
  <action>
    **consultas.routes.ts** (per D-01) — This is the LARGEST module (~1000 lines):
    - GET /api/isp-consultations (line 556)
    - POST /api/isp-consultations (line 568) — the main ISP consultation with score calculation
    - POST /api/isp-consultations/lote — batch consultation (find exact line)
    - GET /api/spc-consultations (line 1585)
    - POST /api/spc-consultations (line 1597)

    This module imports: calculateIspScore, getRiskTier, getDecisionReco from ../score-engine, LGPD masking functions from ../lgpd-masking, storage. Copy the FULL handler logic for POST /api/isp-consultations — it contains the core score calculation flow (~1000 lines of consultation logic). Do NOT simplify.

    **antifraude.routes.ts** (per D-01) — lines ~1783-1880:
    - GET /api/anti-fraud/alerts (line 1783)
    - PATCH /api/anti-fraud/alerts/:id/status (line 1792)
    - GET /api/anti-fraud/customer-risk (line 1809)
    - GET /api/anti-fraud/rules — find in routes.ts
    - PUT /api/anti-fraud/rules — find in routes.ts
    - PATCH /api/anti-fraud/rules/:id/toggle — find in routes.ts

    **heatmap.routes.ts** (per D-01) — lines ~2406-2477:
    - GET /api/config/maps-key (line 2406)
    - GET /api/heatmap/provider (line 2411)
    - GET /api/heatmap/regional (line 2420)
    - GET /api/heatmap/city-ranking (line 2448)

    Imports needed: heatmap-cache functions, geocoding if used.

    CRITICAL: The consultas module is the heart of the application. Every line of the ISP consultation handler must be preserved exactly. The score calculation, credit deduction, LGPD masking, anti-fraud alert generation — all must remain identical.

    Multi-tenant isolation (per D-09, D-10): All endpoints use `req.session.providerId` — preserve exactly.
  </action>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>3 route files exist, consultas contains the full ISP/SPC consultation logic, antifraude has all fraud endpoints, heatmap has all map endpoints, TypeScript compiles</acceptance_criteria>
  <done>consultas (~1000 lines with score calculation), antifraude (6 endpoints), heatmap (4 endpoints) extracted with identical handler logic</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. All 7 route files export a function returning Express Router
3. Each endpoint from routes.ts lines 139-1890 and 2406-2477 appears in exactly one route module
4. No route handler logic was modified — only `app.` changed to `router.`
5. All requireAuth/requireAdmin middleware calls preserved on correct endpoints
</verification>

<success_criteria>
- 7 route modules created under server/routes/
- All provider-facing endpoints (auth, dashboard, import, consultas, antifraude, equipamentos, heatmap) extracted
- Handler logic is byte-for-byte identical (only app->router change)
- TypeScript compiles successfully
- Multi-tenant providerId filtering preserved in every handler
</success_criteria>

<output>
After completion, create `.planning/phases/03-backend-modularization/03-02-SUMMARY.md`
</output>
