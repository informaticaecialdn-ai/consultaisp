---
phase: 01-regionalizacao
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - client/src/pages/configuracoes-regionalizacao.tsx
  - client/src/components/app-sidebar.tsx
autonomous: false
requirements: [REG-02]

must_haves:
  truths:
    - "Provider admin can see a Regionalizacao page listing their configured cities"
    - "Provider admin can search and add cities via autocomplete (minimum 2 chars)"
    - "Provider admin can remove cities from their coverage area"
    - "Changes persist to the database via PUT /api/regional/cidades"
    - "Admin sidebar has a link to the Regionalizacao page"
  artifacts:
    - path: "client/src/pages/configuracoes-regionalizacao.tsx"
      provides: "City configuration UI with autocomplete"
      min_lines: 80
    - path: "client/src/components/app-sidebar.tsx"
      provides: "Navigation link to regionalizacao page"
      contains: "regionalizacao"
  key_links:
    - from: "client/src/pages/configuracoes-regionalizacao.tsx"
      to: "/api/regional/cities"
      via: "fetch for autocomplete"
      pattern: "api/regional/cities"
    - from: "client/src/pages/configuracoes-regionalizacao.tsx"
      to: "/api/regional/cidades"
      via: "PUT to save cities"
      pattern: "api/regional/cidades"
    - from: "client/src/pages/configuracoes-regionalizacao.tsx"
      to: "/api/regional/my-cidades"
      via: "GET to load current cities"
      pattern: "api/regional/my-cidades"
---

<objective>
Build the admin UI for configuring provider coverage areas (cidadesAtendidas) with city autocomplete, and wire it into the sidebar navigation.

Purpose: Allows provider admins to define which cities they serve (REG-02), enabling the regional matching that Phase 2 depends on.
Output: Regionalizacao settings page with city multi-select autocomplete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-regionalizacao/01-01-SUMMARY.md

@client/src/components/app-sidebar.tsx
@client/src/lib/auth.tsx
@client/src/lib/queryClient.ts
@client/src/App.tsx
</context>

<interfaces>
<!-- API contracts from Plan 01 -->

GET /api/regional/cities?q=lon&limit=20
Response: [{ "label": "Londrina - PR", "value": "Londrina - PR", "ibge": "4113700" }]

GET /api/regional/my-cidades
Response: { "cidadesAtendidas": ["Londrina - PR", "Cambe - PR"] }

PUT /api/regional/cidades
Body: { "cidades": ["Londrina - PR", "Cambe - PR"] }
Response: { "cidadesAtendidas": ["Londrina - PR", "Cambe - PR"] }

GET /api/regional/providers
Response: [{ "id": 2, "name": "Provider X", "cidadesAtendidas": ["Londrina - PR"] }]

<!-- Existing patterns -->
From client/src/lib/queryClient.ts:
```typescript
export async function apiRequest(method: string, url: string, body?: any): Promise<Response>
```

From client/src/lib/auth.tsx:
```typescript
export function useAuth(): { user: User | null; isLoading: boolean }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Regionalizacao settings page with city autocomplete</name>
  <read_first>client/src/pages/admin-provedor.tsx (first 100 lines for patterns), client/src/components/app-sidebar.tsx, client/src/App.tsx (route definitions)</read_first>
  <files>client/src/pages/configuracoes-regionalizacao.tsx, client/src/components/app-sidebar.tsx, client/src/App.tsx</files>
  <action>
    **1. Create Regionalizacao page (per D-02, REG-02):**
    Create `client/src/pages/configuracoes-regionalizacao.tsx` following the existing page patterns (useQuery, useMutation, Card, etc.):

    Structure:
    - Page title: "Regionalizacao" with subtitle "Configure as cidades atendidas pelo seu provedor"
    - **Current cities section**: Display current cidadesAtendidas as Badge chips with X button to remove each
    - **Add city section**: Input field with debounced autocomplete (300ms). When user types 2+ chars, call `GET /api/regional/cities?q={input}`. Show results in a dropdown/command list. On select, add city to local state array.
    - **Save button**: Calls `PUT /api/regional/cidades` with the updated array. Show toast on success/error.
    - **Regional providers section**: Below the city config, show a read-only list of providers that share cities (from `GET /api/regional/providers`). Display as cards with provider name and shared cities highlighted.

    Implementation details:
    - Use `useQuery` with key `["my-cidades"]` to load current cities
    - Use `useMutation` for saving, with `queryClient.invalidateQueries(["my-cidades"])` on success
    - For autocomplete, use a controlled Input + local state for search results. Use `useQuery` with `enabled: searchTerm.length >= 2` and a debounced search term (use setTimeout/clearTimeout pattern or a simple state debounce).
    - Use shadcn/ui components: Card, Badge, Button, Input, Command (from cmdk -- already installed). The Command component is ideal for autocomplete.
    - Use `apiRequest` from `@/lib/queryClient` for all API calls (existing project pattern).
    - Show `MapPin` icon from lucide-react for cities, `Users` icon for regional providers section.
    - If no cities configured, show an empty state: "Nenhuma cidade configurada. Adicione as cidades onde seu provedor atua."
    - If no regional providers found, show: "Nenhum provedor regional encontrado. Outros provedores que atendem as mesmas cidades aparecerão aqui."

    **2. Add route to App.tsx:**
    Find the existing route definitions (look for `<Route>` components). Add:
    ```tsx
    <Route path="/configuracoes/regionalizacao" component={ConfiguracoesRegionalizacao} />
    ```
    Import the page component with lazy loading if that's the existing pattern, otherwise direct import.

    **3. Add sidebar link:**
    In `client/src/components/app-sidebar.tsx`, find the admin/settings navigation section. Add a link:
    - Label: "Regionalizacao"
    - Icon: MapPin (from lucide-react)
    - Path: "/configuracoes/regionalizacao"
    - Place it near other settings/config links

    Follow the exact sidebar item structure used for existing links (look for the pattern -- likely an array of objects with name, icon, href).
  </action>
  <acceptance_criteria>
    - Page renders with city list and autocomplete input
    - Typing "Lon" shows Londrina and other matching cities
    - Adding a city shows it as a badge chip
    - Removing a city (X button) removes the badge
    - Save button persists to database
    - Regional providers section shows overlapping providers
    - Sidebar has Regionalizacao link visible to admin role
  </acceptance_criteria>
  <verify>
    <automated>cd C:/ClaudeCode/Consulta_ISP && npx tsc --noEmit 2>&1 | head -20 && grep -q 'regionalizacao' client/src/components/app-sidebar.tsx && echo 'OK: sidebar link' || echo 'FAIL: sidebar link' && grep -q 'regionalizacao' client/src/App.tsx && echo 'OK: route defined' || echo 'FAIL: route missing'</automated>
  </verify>
  <done>
    - Regionalizacao page accessible at /configuracoes/regionalizacao
    - City autocomplete fetches from /api/regional/cities with 2-char minimum
    - Cities displayed as removable badges, saved via PUT /api/regional/cidades
    - Regional providers displayed below city config
    - Sidebar link present for admin users
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify regionalization flow end-to-end</name>
  <action>
    Human verifies the complete regionalization flow: provider admin configures cities via autocomplete, system finds regional providers with overlapping coverage areas.

    Steps to verify:
    1. Start the dev server: `npm run dev`
    2. Log in as a provider admin
    3. Navigate to Configuracoes > Regionalizacao in the sidebar
    4. Verify the page loads with city autocomplete input
    5. Type "Lon" in the search -- verify Londrina and other cities appear
    6. Select 2-3 cities -- verify they appear as badge chips
    7. Click Save -- verify success toast appears
    8. Refresh the page -- verify cities persist
    9. Remove a city by clicking X -- verify badge disappears
    10. Save again -- verify the removal persists
    11. If another provider has overlapping cities, verify they appear in the "Provedores Regionais" section below
  </action>
  <verify>Human visual verification of the complete flow</verify>
  <done>User confirms the regionalization UI works correctly end-to-end</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- Page accessible via sidebar navigation
- Autocomplete responds to typing with filtered city results
- Save + refresh cycle preserves city selection
- Regional providers section shows correct overlapping data
</verification>

<success_criteria>
- Provider admin can configure cities via autocomplete UI (REG-02)
- Cities persist and are editable at any time
- Regional providers with overlapping cities visible on the same page
- Full round-trip: configure cities -> save -> view regional providers works end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-regionalizacao/01-02-SUMMARY.md`
</output>
