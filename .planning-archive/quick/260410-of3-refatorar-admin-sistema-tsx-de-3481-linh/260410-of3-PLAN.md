---
phase: quick-260410-of3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/admin/admin-sistema.tsx
  - client/src/components/app-sidebar.tsx
  - client/src/components/admin/constants.ts
  - client/src/components/admin/ChatPanel.tsx
  - client/src/components/admin/NewProviderWizard.tsx
  - client/src/components/admin/ProviderDrawer.tsx
  - client/src/components/admin/InvoiceTable.tsx
  - client/src/components/admin/tabs/VisaoGeralTab.tsx
  - client/src/components/admin/tabs/ProvedoresTab.tsx
  - client/src/components/admin/tabs/CadastrosTab.tsx
  - client/src/components/admin/tabs/FinanceiroTab.tsx
  - client/src/components/admin/tabs/SuporteTab.tsx
  - client/src/components/admin/tabs/ConfiguracoesTab.tsx
autonomous: false
requirements:
  - QUICK-260410-OF3
must_haves:
  truths:
    - "admin-sistema.tsx shrinks from 3481 lines to ~250 lines (router + layout only)"
    - "Sidebar shows 6 admin items (Painel, Provedores, Cadastros, Financeiro, Suporte, Configuracoes); Usuarios/Integracoes/Sincronizacao Auto removed"
    - "All 25+ API endpoints used by admin panel still respond correctly (no regressions)"
    - "All data-testid attributes preserved so existing tests/selectors keep working"
    - "Provider drawer exposes Dados, Usuarios, Creditos, Plano and ERP sub-tabs in one place"
    - "Suporte tab has two sub-tabs (provider chat + visitor chat) both backed by the generic ChatPanel"
    - "Configuracoes tab manages ERP catalog"
    - "ERP integration mutations use apiRequest/useMutation (no raw fetch)"
    - "npm run check (tsc) passes with zero errors"
  artifacts:
    - path: "client/src/pages/admin/admin-sistema.tsx"
      provides: "Router + layout only, <= 300 lines"
    - path: "client/src/components/admin/tabs/VisaoGeralTab.tsx"
      provides: "Painel overview tab with sincronizacao widget"
    - path: "client/src/components/admin/tabs/ProvedoresTab.tsx"
      provides: "Providers list, opens ProviderDrawer"
    - path: "client/src/components/admin/tabs/CadastrosTab.tsx"
      provides: "Cadastros tab"
    - path: "client/src/components/admin/tabs/FinanceiroTab.tsx"
      provides: "Financeiro tab using InvoiceTable"
    - path: "client/src/components/admin/tabs/SuporteTab.tsx"
      provides: "Suporte tab with provider + visitor sub-tabs using ChatPanel"
    - path: "client/src/components/admin/tabs/ConfiguracoesTab.tsx"
      provides: "ERP catalog management"
    - path: "client/src/components/admin/ChatPanel.tsx"
      provides: "Generic chat panel (merges old ChatPanel + VisitorChatPanel)"
    - path: "client/src/components/admin/NewProviderWizard.tsx"
      provides: "Extracted wizard for creating providers"
    - path: "client/src/components/admin/ProviderDrawer.tsx"
      provides: "Drawer with Dados/Usuarios/Creditos/Plano/ERP sub-tabs"
    - path: "client/src/components/admin/InvoiceTable.tsx"
      provides: "Invoice table used by FinanceiroTab"
    - path: "client/src/components/admin/constants.ts"
      provides: "PAGE_META and shared constants"
    - path: "client/src/components/app-sidebar.tsx"
      provides: "Updated admin nav (6 items)"
  key_links:
    - from: "client/src/pages/admin/admin-sistema.tsx"
      to: "client/src/components/admin/tabs/*"
      via: "dynamic import or direct import + wouter route switch by tab param"
      pattern: "from.*components/admin/tabs/"
    - from: "client/src/components/admin/tabs/SuporteTab.tsx"
      to: "client/src/components/admin/ChatPanel.tsx"
      via: "ChatPanel used twice with different channel props"
      pattern: "ChatPanel"
    - from: "client/src/components/admin/tabs/ProvedoresTab.tsx"
      to: "client/src/components/admin/ProviderDrawer.tsx"
      via: "Open drawer on row click; drawer hosts Dados/Usuarios/Creditos/Plano/ERP"
      pattern: "ProviderDrawer"
    - from: "client/src/components/admin/ProviderDrawer.tsx"
      to: "apiRequest + useMutation"
      via: "ERP config saved via useMutation (no raw fetch)"
      pattern: "useMutation"
---

<objective>
Refactor the monolithic `client/src/pages/admin/admin-sistema.tsx` (3,481 lines, 146K) into focused, maintainable modules while preserving 100% of current behavior, API wiring and data-testid attributes. Consolidate the 9 original tabs into 6 (Painel, Provedores, Cadastros, Financeiro, Suporte, Configuracoes), merge ChatPanel + VisitorChatPanel into a single generic ChatPanel, and push Usuarios/Integracoes/Sincronizacao Auto into the ProviderDrawer sub-tabs.

Purpose: The current file is unmaintainable (30+ useState, 9 tabs, 2 near-duplicate 300+ line chat components, raw fetch calls bypassing React Query). Splitting it unblocks further admin work and reduces context cost of every future edit.

Output: 13 new/updated files under `client/src/components/admin/**` and an updated sidebar + router page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@client/src/pages/admin/admin-sistema.tsx
@client/src/components/app-sidebar.tsx
@client/src/lib/queryClient.ts

<interfaces>
<!-- Conventions the executor must honor (from CLAUDE.md section 13) -->
- Use Wouter (useLocation, Link) for routing — NOT React Router.
- Use TanStack Query (useQuery, useMutation) with `apiRequest` from `@/lib/queryClient` — NO raw fetch in components.
- Use shadcn/ui components and existing patterns (Tabs, Dialog, Drawer/Sheet, Table, Button).
- Preserve all Portuguese (pt-BR) user-facing strings exactly.
- Preserve every `data-testid` attribute on moved elements (copy verbatim).
- Multi-tenant isolation is already server-side; do not change API paths.

<!-- Source map already audited — use these line ranges when extracting -->
admin-sistema.tsx current layout:
  - lines 91-438   : ChatPanel (provider chat)  -> becomes generic ChatPanel
  - lines 452-774  : NewProviderWizard          -> NewProviderWizard.tsx
  - lines 777-1015 : VisitorChatPanel (95% dup) -> collapsed into generic ChatPanel
  - lines 1018-1115: ProviderCreditsModal       -> into ProviderDrawer (Creditos sub-tab)
  - lines 1120+    : Main AdminSistema component (30+ useState)
  - lines 1132-1135: changeTab useCallback      -> DEAD CODE, delete
  - lines 1169-1217: raw fetch() ERP integration -> convert to useMutation in ProviderDrawer (ERP sub-tab)
  - lines 1628-1638: PAGE_META                  -> constants.ts
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared primitives (constants, ChatPanel, NewProviderWizard, ProviderDrawer, InvoiceTable)</name>
  <files>
    client/src/components/admin/constants.ts,
    client/src/components/admin/ChatPanel.tsx,
    client/src/components/admin/NewProviderWizard.tsx,
    client/src/components/admin/ProviderDrawer.tsx,
    client/src/components/admin/InvoiceTable.tsx
  </files>
  <action>
    Create the shared primitives that multiple tabs will consume. Work bottom-up so later tasks just import.

    1. `constants.ts`: Move PAGE_META (admin-sistema.tsx lines 1628-1638) plus any other constants referenced across tabs. Export as named consts.

    2. `ChatPanel.tsx` (generic): Merge ChatPanel (lines 91-438) and VisitorChatPanel (lines 777-1015). They are 95% duplicate — parameterize via props:
       ```ts
       type ChatVariant = "provider" | "visitor";
       interface ChatPanelProps {
         variant: ChatVariant;
         threadsEndpoint: string;   // e.g. "/api/admin/chat/threads" | "/api/admin/visitor-chats"
         messagesEndpoint: (threadId: string | number) => string;
         sendEndpoint: (threadId: string | number) => string;
         title?: string;
       }
       ```
       Keep every data-testid. Keep WebSocket wiring if present in original. Use apiRequest + useQuery/useMutation, no raw fetch. Keep pt-BR strings.

    3. `NewProviderWizard.tsx`: Lift-and-shift lines 452-774 verbatim. Export default. Make sure any shared types come from `@shared/schema` (do not duplicate). Preserve all testids and the exact step flow.

    4. `ProviderDrawer.tsx`: New component that consolidates what used to be separate Usuarios, Integracoes and ProviderCreditsModal surfaces. Internal shadcn Tabs with 5 sub-tabs: `Dados`, `Usuarios`, `Creditos`, `Plano`, `ERP`.
       - `Dados`: edit provider profile (endpoints currently used in admin-sistema for provider detail).
       - `Usuarios`: list/create/delete users for that provider.
       - `Creditos`: move ProviderCreditsModal body (lines 1018-1115) inside.
       - `Plano`: change plan (existing endpoints).
       - `ERP`: move ERP integration form. CRITICAL: convert the raw `fetch()` at lines 1169-1217 into a `useMutation` using `apiRequest`. Preserve endpoint, method, body, success/error toasts and testids exactly.
       Props: `{ providerId: number | null; open: boolean; onOpenChange: (o: boolean) => void }`. Use shadcn Sheet (drawer).

    5. `InvoiceTable.tsx`: Extract the invoice listing/table UI currently inside the Financeiro tab of admin-sistema.tsx. Accept data via props or fetch internally with useQuery — match whatever the original does. Keep every testid and column.

    Do NOT touch admin-sistema.tsx yet (that is Task 2). Do NOT delete original code yet — we need it as reference while writing these modules.

    Per project rules: wouter, apiRequest, TanStack Query, shadcn/ui, pt-BR strings, preserve data-testid.
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    Five new files exist under client/src/components/admin/. `npm run check` (tsc) passes. No raw fetch() remains in any of the new files (grep returns zero). All moved data-testid attributes are present in the new files.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 6 tab modules, slim admin-sistema.tsx to router/layout, update sidebar</name>
  <files>
    client/src/components/admin/tabs/VisaoGeralTab.tsx,
    client/src/components/admin/tabs/ProvedoresTab.tsx,
    client/src/components/admin/tabs/CadastrosTab.tsx,
    client/src/components/admin/tabs/FinanceiroTab.tsx,
    client/src/components/admin/tabs/SuporteTab.tsx,
    client/src/components/admin/tabs/ConfiguracoesTab.tsx,
    client/src/pages/admin/admin-sistema.tsx,
    client/src/components/app-sidebar.tsx
  </files>
  <action>
    Split the body of admin-sistema.tsx into one file per tab, then reduce the page itself to a router/layout.

    1. Create each tab file as a default-exported function component. Move ONLY the JSX + hooks that belong to that tab out of admin-sistema.tsx. Keep every data-testid and pt-BR string.

       - `VisaoGeralTab.tsx` (was `painel`): dashboard cards + embed the sincronizacao widget (merge content from the old Sincronizacao Auto tab — reuse its hooks/queries here).
       - `ProvedoresTab.tsx` (was `provedores`): provider list + search + NewProviderWizard trigger. Opens `ProviderDrawer` on row click. The old Usuarios and Integracoes tabs collapse entirely into the drawer sub-tabs — there is no separate top-level tab for them.
       - `CadastrosTab.tsx`: unchanged content, extracted.
       - `FinanceiroTab.tsx`: uses `InvoiceTable` from Task 1.
       - `SuporteTab.tsx`: shadcn Tabs with two sub-tabs ("Provedores" and "Visitantes"), each rendering `<ChatPanel variant="provider" ... />` and `<ChatPanel variant="visitor" ... />` with the correct endpoints pulled from the original ChatPanel/VisitorChatPanel instantiations.
       - `ConfiguracoesTab.tsx` (NEW): ERP catalog CRUD. Use existing `/api/admin/erp-catalog` endpoints (GET/POST/PATCH/DELETE). Simple table + form via useQuery/useMutation.

       Each tab file should own its own useState/useQuery locally. Do not share state through the parent page unless it is truly cross-tab (e.g., a selected providerId for the drawer — hoist only that if needed).

    2. Rewrite `admin-sistema.tsx` as a thin router/layout (~250 lines max). Responsibilities:
       - Read current tab from URL (wouter `useLocation` + search param, matching existing hash/query convention in the file — preserve it).
       - Render a `Tabs` container with 6 `TabsList` entries: Painel, Provedores, Cadastros, Financeiro, Suporte, Configuracoes.
       - Render the corresponding tab component.
       - Hold ONLY shared state that truly crosses tabs (e.g., selected `providerId` + drawer open state) if not already encapsulated.
       - DELETE the dead `changeTab` useCallback (old lines 1132-1135).
       - Remove all code now living in the extracted files. Remove the old inline ChatPanel, VisitorChatPanel, NewProviderWizard, ProviderCreditsModal definitions.
       - Import PAGE_META from `constants.ts`.

    3. Update `client/src/components/app-sidebar.tsx`:
       - REMOVE nav items: "Usuarios", "Integracoes", "Sincronizacao Auto" (admin section only).
       - ADD nav item: "Configuracoes" that points at the Configuracoes tab of `/admin-sistema` (use the same URL/hash/query scheme the page expects).
       - Preserve icons style consistent with existing items. Preserve all other (non-admin) nav items unchanged. Preserve data-testids.

    Self-check before finishing:
    - No file under client/src/components/admin/** or admin-sistema.tsx contains a raw `fetch(`.
    - `changeTab` no longer exists anywhere.
    - admin-sistema.tsx is <= ~300 lines.
    - Every API endpoint the original file called is still called from some new file (spot-check the 25+ endpoints listed in CLAUDE.md section 9 admin block).
  </action>
  <verify>
    <automated>npm run check</automated>
  </verify>
  <done>
    admin-sistema.tsx reduced to router/layout (<= ~300 lines). Six tab files exist. Sidebar shows the 6-item admin nav (Usuarios/Integracoes/Sincronizacao Auto gone, Configuracoes added). `npm run check` passes. No raw fetch() in admin files. No `changeTab` references remain.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of refactored admin panel</name>
  <what-built>
    Full refactor of admin-sistema.tsx: 13 new/updated files, 6-tab layout, generic ChatPanel, ProviderDrawer with 5 sub-tabs, new Configuracoes tab, sidebar updated. All API endpoints and data-testids preserved.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and log in as superadmin.
    2. Navigate to `/admin-sistema`. Confirm sidebar shows exactly: Painel, Provedores, Cadastros, Financeiro, Suporte, Configuracoes (and that Usuarios, Integracoes, Sincronizacao Auto are gone).
    3. Painel: dashboard cards render AND the sincronizacao auto widget is visible/working.
    4. Provedores:
       - List loads.
       - Click "Novo Provedor" -> wizard opens and can step through (do not submit unless safe).
       - Click an existing provider row -> ProviderDrawer opens with 5 sub-tabs: Dados, Usuarios, Creditos, Plano, ERP.
       - In ERP sub-tab, change a field and save -> toast success, no console errors, Network tab shows the expected endpoint (not a raw fetch regression).
       - In Usuarios sub-tab, list loads.
       - In Creditos sub-tab, form renders (was the old ProviderCreditsModal).
    5. Cadastros: loads unchanged.
    6. Financeiro: invoice table renders, actions work.
    7. Suporte: two sub-tabs ("Provedores" and "Visitantes") both load messages and allow sending a test message.
    8. Configuracoes: ERP catalog list renders; create/edit works.
    9. Open DevTools console -> zero red errors across all tabs.
    10. Spot-check that at least 5 critical data-testid values you remember from the old file still resolve in the DOM.
  </how-to-verify>
  <resume-signal>Type "approved" or list issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run check` passes (tsc).
- `admin-sistema.tsx` line count <= 300 (was 3,481).
- Grep: zero raw `fetch(` calls in `client/src/pages/admin/admin-sistema.tsx` and `client/src/components/admin/**`.
- Grep: zero occurrences of `changeTab` in admin files.
- Sidebar renders 6 admin nav items (Painel, Provedores, Cadastros, Financeiro, Suporte, Configuracoes).
- Manual smoke test (Task 3) passes with zero console errors.
</verification>

<success_criteria>
- admin-sistema.tsx reduced from 3,481 -> ~250-300 lines (router + layout only).
- 13 new/updated files in place matching the target structure.
- 9 tabs consolidated into 6; Usuarios/Integracoes/Sincronizacao Auto merged into ProviderDrawer + Painel.
- Generic ChatPanel replaces both ChatPanel and VisitorChatPanel (duplication eliminated).
- All 25+ admin API endpoints still called from the refactored code.
- All data-testid attributes preserved.
- Raw fetch in ERP integration converted to useMutation via apiRequest.
- `changeTab` dead code removed.
- `npm run check` clean; human smoke test approved.
</success_criteria>

<output>
After completion, create `.planning/quick/260410-of3-refatorar-admin-sistema-tsx-de-3481-linh/260410-of3-SUMMARY.md`
</output>
