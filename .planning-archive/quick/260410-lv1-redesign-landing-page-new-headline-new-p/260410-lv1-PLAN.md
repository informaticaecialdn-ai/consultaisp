---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/pages/public/landingpage.tsx
autonomous: true
requirements: [quick-landing-redesign]
must_haves:
  truths:
    - "Hero displays new headline 'Saiba quem nao vai pagar — antes de instalar.'"
    - "Pricing section shows exactly 2 plans (Gratuito R$0, Profissional R$99/mes) plus per-query pricing"
    - "Page has exactly 7 sections: Hero, Como Funciona, Funcionalidades, Precos, Social Proof, FAQ, CTA Final"
    - "Word 'calote' does not appear anywhere in the file"
    - "CTA final references 40 creditos (not 30)"
    - "File is ~500 lines (significantly shorter than 764)"
  artifacts:
    - path: "client/src/pages/public/landingpage.tsx"
      provides: "Redesigned landing page"
  key_links:
    - from: "client/src/pages/public/landingpage.tsx"
      to: "/api/public/erp-catalog"
      via: "fetch in useEffect"
      pattern: "fetch.*erp-catalog"
---

<objective>
Redesign the landing page with new headline, simplified 2-plan pricing model, and consolidated 7-section structure. Remove redundant sections (Dores, duplicate ERP bar, verbose Funcionalidades mockups) to reduce from 764 to ~500 lines while keeping all functional logic intact.

Purpose: Clearer value proposition, simpler pricing (2 plans + per-query), fewer sections = less bounce.
Output: Rewritten `landingpage.tsx` (~500 lines)
</objective>

<execution_context>
@C:\Users\pc\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\pc\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@client/src/pages/public/landingpage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite landing page with new structure, headline, and pricing</name>
  <files>client/src/pages/public/landingpage.tsx</files>
  <action>
Rewrite `client/src/pages/public/landingpage.tsx` as a single complete file. Keep ALL existing: imports, ErpItem type, fallbackErps, useEffect for ERP catalog fetch, goRegister/goLogin, data-testid attributes, CSS variable system, responsive patterns, LandingChatbot at bottom, Sheet mobile menu.

**SECTION 1 — HERO (keep nav + hero + ERP trust bar after hero):**
- Nav: keep exactly as-is (logo, links, login/register buttons, mobile Sheet menu)
- Hero headline: replace with `Saiba quem nao vai pagar —` (line 1) + `antes de instalar.` (line 2, colored navy). Remove the old 3rd line subheadline.
- Hero paragraph: "A rede colaborativa que revela o historico de inadimplencia de qualquer CPF entre provedores de internet. Score de risco em 2 segundos."
- Keep CTA buttons as-is (Criar conta gratis + Ver como funciona)
- Keep stats bar but update: keep "R$ 690 prejuizo medio/inadimplente", "< 2s resultado da consulta", "Gratis consultas na sua base"
- Keep the mockup (browser window with score result) exactly as-is
- Keep ERP trust bar (lines 198-215) right after hero — this is the ONLY ERP bar

**SECTION 2 — COMO FUNCIONA (keep as-is, lines 217-244):**
- Keep the 3-step section exactly as-is. No changes needed.

**SECTION 3 — FUNCIONALIDADES (NEW — replace Dores + old Funcionalidades + secondary grid):**
- Section id="funcionalidades"
- Header: "Tudo que voce precisa para proteger sua receita"
- Subtitle: "Cada funcionalidade resolve um problema real do dia a dia do provedor."
- 6 benefit cards in a 3x2 grid (md:grid-cols-2 lg:grid-cols-3):
  1. Search icon, navy: "Consulta ISP" — "Score de risco em 2 segundos. Historico de inadimplencia em toda a rede colaborativa, equipamentos retidos e sugestao automatica de decisao."
  2. Bell icon, danger: "Anti-Fraude" — "Alerta via WhatsApp em tempo real quando seu cliente inadimplente e consultado por outro provedor. Detecta migradores seriais."
  3. Router icon, gold: "Controle de Equipamentos" — "Registre ONUs por modelo, serial e status. Rastreie equipamentos em comodato e identifique retencoes antes que virem prejuizo."
  4. MapPin icon, navy: "Consulta por Endereco" — "Cruza CEP + numero em toda a rede. Detecta inadimplencia no imovel mesmo com CPF diferente — identifica golpes de familiares."
  5. CreditCard icon, success: "SPC Integrada" — "Score SPC, restricoes financeiras e protestos direto na plataforma. Negativacao sem contrato adicional com Serasa."
  6. Zap icon, gold: "Analise com IA" — "Recomendacoes automaticas baseadas em inteligencia artificial: APROVAR, APROVAR COM RESSALVAS ou REJEITAR."
- Each card: icon in colored bg circle, title bold, description text-sm muted. Same card style as current secondary grid (border, rounded, p-5, hover:border-navy).
- NO mockup blocks, NO "Resolver este problema" buttons. Just clean cards.

**SECTION 4 — PRECOS (NEW pricing model):**
- Section id="precos"
- Header: "Simples, transparente, sem surpresa"
- Subtitle: "Consultas na sua propria base sao sempre gratuitas. Pague apenas pelo que usar na rede."
- TWO plan cards side by side (max-w-3xl mx-auto, md:grid-cols-2 gap-6):
  - **Gratuito** (not highlighted): R$ 0 "para sempre", "Para conhecer a plataforma"
    Features: "40 creditos para testar a rede", "Consultas ilimitadas na sua base", "Anti-fraude basico", "Importacao via CSV", "1 usuario"
    CTA: "Criar conta gratis" (outline style)
  - **Profissional** (highlighted with RECOMENDADO badge): R$ 99 "/mes", "Acesso completo para seu provedor"
    Features: "Acesso completo a toda a rede", "Todos os ERPs integrados", "Usuarios ilimitados", "Anti-fraude completo + WhatsApp", "Consulta em lote (ate 500 CPFs)", "Suporte prioritario"
    CTA: "Comecar agora" (navy solid style)
- AFTER the cards, add a per-query pricing breakdown box:
  ```
  <div> with bg-surface, border, rounded, p-6, max-w-3xl mx-auto mt-8
    Title: "Custo por consulta" (font-mono uppercase tracking)
    Three rows:
    - CheckCircle2 success: "Consulta na propria base" → "GRATIS" (success color, bold)
    - CheckCircle2 navy: "Consulta colaborativa (outros provedores)" → "R$ 1,00 / CPF" (navy color)
    - CheckCircle2 navy: "Consulta SPC/Serasa" → "R$ 4,00 / CPF" (navy color)
  </div>
  ```
- Keep trust badges below (Sem contrato, Dados LGPD, Cancele quando quiser)

**SECTION 5 — SOCIAL PROOF (merge testimonials + comparison table):**
- Keep the testimonial header + 6 testimonials grid exactly as-is (lines 639-672)
- MOVE the comparison table (lines 509-553) to appear AFTER testimonials in the same section
- Update table header: "Comparativo com o mercado" (smaller, h3 not h2)
- Replace word "calote" if present in testimonials (check: none found, but verify)

**SECTION 6 — FAQ (reduce from 13 to 7):**
- Keep section id="faq"
- Keep ONLY these 7 questions (reuse existing text, update pricing references):
  1. "O que e a base de dados compartilhada?" (keep as-is)
  2. "Consultas na minha propria base sao cobradas?" (keep as-is)
  3. "Como funciona a analise por endereco?" (keep as-is)
  4. "Quanto tempo leva para configurar?" (keep as-is)
  5. "Compartilhar dados de inadimplentes viola a LGPD?" (keep as-is)
  6. "E a Resolucao Anatel 765 — como ela afeta meu provedor?" (keep as-is but replace "calote" if found)
  7. "Quais ERPs sao suportados na integracao automatica?" (keep as-is)

**SECTION 7 — CTA FINAL:**
- Keep navy background section
- Update headline to: "Saiba quem nao vai pagar" + line break + "antes de instalar." (blue-200 color)
- Update body: "Cadastro em 2 minutos. 40 creditos gratuitos para testar a rede. Consultas na sua base sempre gratuitas."
- Keep CTA buttons (Criar conta gratis + Ja tenho conta)
- Update trust signals: "Gratuito na base propria", "Sem contrato de fidelidade", "LGPD compliant"

**FOOTER:** Keep exactly as-is.

**REMOVE ENTIRELY:**
- Dores section (lines 246-328) — folded into Funcionalidades cards
- Old Funcionalidades section with mockup blocks (lines 330-506) — replaced by 6 cards
- Duplicate ERP section (lines 614-637) — keep only trust bar after hero
- 6 FAQ items (keep 7 of 13)

**GLOBAL COPY:**
- Replace ALL instances of "calote" with "inadimplencia" or rephrase
- Replace "30 creditos" with "40 creditos" everywhere
- Remove unused lucide imports (TrendingDown, Globe if no longer used). Keep all others.

**TARGET:** ~500 lines. The major savings come from removing Dores (~80 lines), Funcionalidades mockups (~175 lines), duplicate ERP (~25 lines), and 6 FAQ items (~30 lines).
  </action>
  <verify>
    <automated>cd C:/ClaudeCode && npx tsc --noEmit client/src/pages/public/landingpage.tsx 2>&1 | head -20; echo "---"; wc -l client/src/pages/public/landingpage.tsx; echo "---"; grep -c "calote" client/src/pages/public/landingpage.tsx || echo "0 calote instances"; echo "---"; grep -c "40 créditos\|40 creditos" client/src/pages/public/landingpage.tsx; echo "---"; grep "Saiba quem" client/src/pages/public/landingpage.tsx | head -3; echo "---"; grep "R\$ 99" client/src/pages/public/landingpage.tsx | head -3</automated>
  </verify>
  <done>
- File compiles with no TypeScript errors
- File is ~450-550 lines (down from 764)
- Zero instances of "calote"
- New headline "Saiba quem nao vai pagar" present
- New pricing R$99/mes present with 2 plans only
- 7 sections total (Hero, Como Funciona, Funcionalidades, Precos, Social Proof, FAQ, CTA Final)
- All data-testid attributes preserved
- LandingChatbot still at bottom
- ERP catalog fetch still works
- goRegister/goLogin still work
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Redesigned landing page with new headline, 2-plan pricing, and 7 consolidated sections</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:5000/landingpage
    2. Verify hero shows "Saiba quem nao vai pagar — antes de instalar."
    3. Scroll to Funcionalidades — should show 6 clean benefit cards (no mockups)
    4. Scroll to Precos — should show 2 plans (Gratuito R$0, Profissional R$99/mes) + per-query pricing
    5. Verify FAQ has exactly 7 questions
    6. Verify CTA final says "40 creditos"
    7. Check mobile responsiveness (resize browser)
    8. Ctrl+F for "calote" — should find 0 results
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compiles without errors
- File line count between 450-550
- No "calote" in file
- New headline present
- 2-plan pricing with R$99 present
- All data-testid preserved
- LandingChatbot rendered
</verification>

<success_criteria>
Landing page redesigned: new headline, 2-plan pricing model with per-query breakdown, 7 sections (down from 13), ~500 lines (down from 764), zero "calote", all functional logic preserved.
</success_criteria>

<output>
After completion, create `.planning/quick/260410-lv1-redesign-landing-page-new-headline-new-p/260410-lv1-SUMMARY.md`
</output>
