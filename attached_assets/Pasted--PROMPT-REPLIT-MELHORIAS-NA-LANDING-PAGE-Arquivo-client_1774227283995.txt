# PROMPT REPLIT — MELHORIAS NA LANDING PAGE
# Arquivo: client/src/pages/landingpage.tsx
# Cole no chat do Replit Agent

---

Preciso que você aplique as seguintes melhorias na landing page do Consulta ISP.
O arquivo é `client/src/pages/landingpage.tsx`.

**IMPORTANTE**: Não altere nenhuma outra página. Apenas a landing page.
Mantenha todos os imports existentes e adicione os novos necessários.

---

## MUDANÇA 1 — Adicionar "Preços" no menu de navegação

Localizar o bloco de links da nav:
```tsx
<button onClick={() => document.getElementById("como-funciona")?.scrollIntoView(...)} ...>Como funciona</button>
<button onClick={() => document.getElementById("funcionalidades")?.scrollIntoView(...)} ...>Funcionalidades</button>
<button onClick={() => document.getElementById("faq")?.scrollIntoView(...)} ...>FAQ</button>
```

Adicionar botão "Preços" entre "Funcionalidades" e "FAQ":
```tsx
<button
  onClick={() => document.getElementById("precos")?.scrollIntoView({ behavior: "smooth" })}
  className="hover:text-blue-600 transition-colors"
  data-testid="nav-precos"
>
  Preços
</button>
```

---

## MUDANÇA 2 — Reescrever o Hero section completamente

Substituir TODO o conteúdo da `<div>` da esquerda dentro do grid do hero (a div que começa com o badge "Base de dados compartilhada entre provedores"):

```tsx
<div>
  {/* Badge urgência */}
  <div className="inline-flex items-center gap-2 bg-red-50 border border-red-200 text-red-700 text-xs font-semibold px-3 py-1.5 rounded-full mb-4">
    <AlertTriangle className="w-3.5 h-3.5" />
    73 milhões de brasileiros negativados em 2025 — recorde histórico
  </div>

  {/* Headline direto na dor */}
  <h1
    className="text-4xl sm:text-5xl lg:text-6xl font-black text-slate-900 leading-[1.1] tracking-tight"
    data-testid="text-hero-title"
  >
    Cada calote custa{" "}
    <span className="text-red-500">R$ 400.</span>
    <br />
    Cada ONU perdida,{" "}
    <span className="text-red-500">R$ 300.</span>
    <br />
    <span className="text-blue-600">Consulte o CPF antes</span>
    <br />
    de instalar.
  </h1>

  <p className="text-lg text-slate-500 mt-5 leading-relaxed max-w-lg">
    Rede colaborativa de inadimplentes entre provedores. Consulte CPF/CNPJ
    em segundos e saiba se o cliente tem histórico de calote em{" "}
    <strong className="text-slate-700">qualquer provedor da rede</strong>{" "}
    antes de liberar o contrato.
  </p>

  {/* Banner de dado de mercado */}
  <div className="flex items-start gap-3 bg-amber-50 border border-amber-200 rounded-xl p-4 mt-6">
    <AlertTriangle className="w-5 h-5 text-amber-600 flex-shrink-0 mt-0.5" />
    <div className="text-sm text-amber-800">
      <strong>Cenário atual do setor:</strong> ISPs regionais operam com taxas de inadimplência de{" "}
      <strong>10% a 30%</strong> — 6x acima do limite saudável. O custo médio de ativação FTTH é de{" "}
      <strong>R$ 400 por cliente</strong>. Cada inadimplente representa{" "}
      <strong>prejuízo médio de R$ 690</strong> entre dívida e equipamento.
    </div>
  </div>

  {/* CTAs */}
  <div className="flex flex-col sm:flex-row gap-3 mt-8">
    <Button
      size="lg"
      className="bg-blue-600 hover:bg-blue-700 text-white px-8 gap-2 h-12 text-base font-bold"
      onClick={goRegister}
      data-testid="button-hero-cta"
    >
      Criar Conta Grátis
      <ArrowRight className="w-4 h-4" />
    </Button>
    <Button
      size="lg"
      variant="outline"
      className="border-slate-300 text-slate-600 px-8 gap-2 h-12 text-base"
      onClick={() => document.getElementById("calculadora")?.scrollIntoView({ behavior: "smooth" })}
      data-testid="button-hero-calculadora"
    >
      <TrendingUp className="w-4 h-4" />
      Calcular minha economia
    </Button>
  </div>

  {/* Stats */}
  <div className="flex items-center gap-6 mt-10 pt-8 border-t border-slate-200">
    {[
      { v: "19.000+", l: "ISPs no Brasil" },
      { v: "R$ 690", l: "Prejuízo/inadimplente" },
      { v: "< 2s", l: "Resultado" },
    ].map((s) => (
      <div key={s.l}>
        <p className="text-2xl font-black text-slate-900">{s.v}</p>
        <p className="text-xs text-slate-400">{s.l}</p>
      </div>
    ))}
  </div>
</div>
```

---

## MUDANÇA 3 — Nova seção "Calculadora de Economia" (após o Hero, antes da seção base compartilhada)

Adicionar `useState` para os valores da calculadora no início do componente:
```tsx
const [calcClientes, setCalcClientes] = useState(300);
const [calcTaxa, setCalcTaxa] = useState(10);
const [calcEquip, setCalcEquip] = useState(2);
```

Inserir a seção logo APÓS o `</section>` do hero e ANTES da `<section className="py-16 bg-slate-50">` (seção da base compartilhada):

```tsx
{/* CALCULADORA DE ECONOMIA */}
<section id="calculadora" className="py-20 bg-white border-t border-slate-100">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div className="text-center mb-12">
      <div className="inline-flex items-center gap-2 text-green-600 text-xs font-bold uppercase tracking-widest mb-3">
        <TrendingUp className="w-4 h-4" />
        Calculadora de Economia
      </div>
      <h2 className="text-3xl sm:text-4xl font-black text-slate-900">
        Quanto você está perdendo por mês?
      </h2>
      <p className="text-slate-500 mt-3 max-w-xl mx-auto">
        Ajuste os valores do seu provedor e veja o impacto financeiro da inadimplência.
      </p>
    </div>

    <div className="grid lg:grid-cols-2 gap-12 items-center">
      {/* Sliders */}
      <div className="space-y-8">
        <div>
          <div className="flex justify-between items-center mb-3">
            <label className="text-sm font-semibold text-slate-700">
              Clientes ativos
            </label>
            <span className="text-2xl font-black text-blue-600">{calcClientes.toLocaleString('pt-BR')}</span>
          </div>
          <input
            type="range" min={50} max={5000} step={50}
            value={calcClientes}
            onChange={(e) => setCalcClientes(Number(e.target.value))}
            className="w-full h-2 bg-slate-200 rounded-full appearance-none cursor-pointer accent-blue-600"
          />
          <div className="flex justify-between text-xs text-slate-400 mt-1">
            <span>50</span><span>5.000</span>
          </div>
        </div>

        <div>
          <div className="flex justify-between items-center mb-3">
            <label className="text-sm font-semibold text-slate-700">
              Taxa de inadimplência estimada
            </label>
            <span className="text-2xl font-black text-red-500">{calcTaxa}%</span>
          </div>
          <input
            type="range" min={2} max={40} step={1}
            value={calcTaxa}
            onChange={(e) => setCalcTaxa(Number(e.target.value))}
            className="w-full h-2 bg-slate-200 rounded-full appearance-none cursor-pointer accent-red-500"
          />
          <div className="flex justify-between text-xs text-slate-400 mt-1">
            <span>2% (ótimo)</span><span>40% (crítico)</span>
          </div>
        </div>

        <div>
          <div className="flex justify-between items-center mb-3">
            <label className="text-sm font-semibold text-slate-700">
              Equipamentos em comodato por cliente
            </label>
            <span className="text-2xl font-black text-amber-500">{calcEquip} equip.</span>
          </div>
          <input
            type="range" min={1} max={4} step={1}
            value={calcEquip}
            onChange={(e) => setCalcEquip(Number(e.target.value))}
            className="w-full h-2 bg-slate-200 rounded-full appearance-none cursor-pointer accent-amber-500"
          />
          <div className="flex justify-between text-xs text-slate-400 mt-1">
            <span>1 (só ONU)</span><span>4 (ONU + roteador + switch)</span>
          </div>
        </div>
      </div>

      {/* Resultados */}
      <div className="space-y-4">
        {(() => {
          const inadimplentes = Math.round(calcClientes * (calcTaxa / 100));
          const prejDivida = inadimplentes * 149; // ticket médio R$149/mês
          const prejEquip = inadimplentes * calcEquip * 290; // R$290/equip médio
          const prejTotal = prejDivida + prejEquip;
          const economia = Math.round(prejTotal * 0.70);
          const custoPlano = 149;
          const roi = Math.round(economia / custoPlano);

          return (
            <>
              <div className="bg-red-50 border border-red-200 rounded-2xl p-5">
                <p className="text-xs font-semibold text-red-500 uppercase tracking-wide mb-1">
                  Inadimplentes estimados
                </p>
                <p className="text-4xl font-black text-red-600">
                  {inadimplentes.toLocaleString('pt-BR')}
                  <span className="text-lg font-semibold text-red-400 ml-1">clientes</span>
                </p>
                <p className="text-sm text-red-400 mt-1">
                  com {calcTaxa}% de taxa sobre {calcClientes.toLocaleString('pt-BR')} clientes
                </p>
              </div>

              <div className="grid grid-cols-2 gap-4">
                <div className="bg-orange-50 border border-orange-200 rounded-xl p-4">
                  <p className="text-xs font-semibold text-orange-500 uppercase tracking-wide mb-1">
                    Perda em dívidas/mês
                  </p>
                  <p className="text-2xl font-black text-orange-600">
                    {prejDivida.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL', maximumFractionDigits: 0 })}
                  </p>
                </div>
                <div className="bg-amber-50 border border-amber-200 rounded-xl p-4">
                  <p className="text-xs font-semibold text-amber-600 uppercase tracking-wide mb-1">
                    Equipamentos em risco
                  </p>
                  <p className="text-2xl font-black text-amber-700">
                    {prejEquip.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL', maximumFractionDigits: 0 })}
                  </p>
                </div>
              </div>

              <div className="bg-slate-900 rounded-2xl p-6">
                <p className="text-xs font-semibold text-slate-400 uppercase tracking-wide mb-1">
                  Prejuízo total estimado/mês
                </p>
                <p className="text-5xl font-black text-white">
                  {prejTotal.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL', maximumFractionDigits: 0 })}
                </p>
                <div className="border-t border-slate-700 mt-4 pt-4">
                  <p className="text-xs font-semibold text-green-400 uppercase tracking-wide mb-1">
                    Com Consulta ISP você evita até 70%
                  </p>
                  <p className="text-3xl font-black text-green-400">
                    {economia.toLocaleString('pt-BR', { style: 'currency', currency: 'BRL', maximumFractionDigits: 0 })}
                    <span className="text-lg font-semibold text-green-500 ml-1">/mês protegidos</span>
                  </p>
                  <p className="text-xs text-slate-500 mt-2">
                    ROI estimado: <strong className="text-white">{roi}x</strong> o custo do plano Básico (R$ {custoPlano}/mês)
                  </p>
                </div>
              </div>

              <Button
                size="lg"
                className="w-full bg-blue-600 hover:bg-blue-700 text-white gap-2 h-12 text-base font-bold"
                onClick={goRegister}
              >
                Proteger meu provedor agora
                <ArrowRight className="w-4 h-4" />
              </Button>
            </>
          );
        })()}
      </div>
    </div>
  </div>
</section>
```

---

## MUDANÇA 4 — Nova seção "Anti-Fraude em ação" (após a seção "Base Compartilhada", antes de "Como funciona")

Inserir ANTES do `<section id="como-funciona" ...>`:

```tsx
{/* ANTI-FRAUDE SEÇÃO DEDICADA */}
<section className="py-20 bg-slate-900 overflow-hidden relative">
  <div className="absolute inset-0">
    <div className="absolute top-20 left-1/4 w-96 h-96 bg-red-900/20 rounded-full blur-3xl" />
    <div className="absolute bottom-10 right-1/4 w-80 h-80 bg-blue-900/20 rounded-full blur-3xl" />
  </div>
  <div className="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div className="text-center mb-14">
      <div className="inline-flex items-center gap-2 text-red-400 text-xs font-bold uppercase tracking-widest mb-3">
        <AlertTriangle className="w-4 h-4" />
        Anti-Fraude em Tempo Real
      </div>
      <h2 className="text-3xl sm:text-4xl font-black text-white">
        Seu inadimplente tentou contratar<br />em outro provedor.{" "}
        <span className="text-red-400">Você fica sabendo agora.</span>
      </h2>
      <p className="text-slate-400 mt-4 max-w-2xl mx-auto text-lg">
        70% dos churns são por mudança de endereço. Sem o anti-fraude, você só descobre
        quando o cliente já foi embora — com sua ONU.
      </p>
    </div>

    {/* Timeline visual */}
    <div className="grid grid-cols-1 md:grid-cols-4 gap-0 items-center max-w-4xl mx-auto mb-14">
      {[
        {
          icon: "😤",
          title: "Cliente inadimplente",
          desc: "R$ 850 em aberto. 95 dias de atraso. ONU não devolvida.",
          color: "border-red-500/40 bg-red-950/40",
          textColor: "text-red-400",
        },
        {
          icon: "🚪",
          title: "Tenta migrar",
          desc: "Vai contratar internet em outro provedor da sua cidade.",
          color: "border-amber-500/40 bg-amber-950/40",
          textColor: "text-amber-400",
        },
        {
          icon: "🔍",
          title: "Novo provedor consulta",
          desc: "O novo provedor usa o Consulta ISP para checar o CPF.",
          color: "border-blue-500/40 bg-blue-950/40",
          textColor: "text-blue-400",
        },
        {
          icon: "🚨",
          title: "Você recebe o alerta",
          desc: "Notificação imediata: seu inadimplente está tentando migrar.",
          color: "border-green-500/40 bg-green-950/40",
          textColor: "text-green-400",
        },
      ].map((step, i) => (
        <div key={i} className="flex items-center gap-0">
          <div className={`flex-1 border ${step.color} rounded-2xl p-5 text-center`}>
            <div className="text-4xl mb-3">{step.icon}</div>
            <h3 className={`text-sm font-bold ${step.textColor} mb-2`}>{step.title}</h3>
            <p className="text-xs text-slate-400 leading-relaxed">{step.desc}</p>
          </div>
          {i < 3 && (
            <div className="hidden md:flex px-2 text-slate-600 text-2xl flex-shrink-0">
              →
            </div>
          )}
        </div>
      ))}
    </div>

    {/* Stats da seção */}
    <div className="grid grid-cols-1 sm:grid-cols-3 gap-6 max-w-3xl mx-auto">
      {[
        { stat: "70%", desc: "dos churns são por mudança de endereço — o cliente leva o equipamento" },
        { stat: "< 5s", desc: "para você receber o alerta anti-fraude por WhatsApp ou email" },
        { stat: "R$ 690", desc: "é o prejuízo médio por inadimplente entre dívida e equipamento retido" },
      ].map((s, i) => (
        <div key={i} className="text-center bg-white/5 border border-white/10 rounded-2xl p-6">
          <p className="text-4xl font-black text-white mb-2">{s.stat}</p>
          <p className="text-sm text-slate-400 leading-relaxed">{s.desc}</p>
        </div>
      ))}
    </div>
  </div>
</section>
```

---

## MUDANÇA 5 — Atualizar seção "Como funciona" com dados concretos

Substituir o array de steps pelo seguinte:
```tsx
{[
  {
    n: "1",
    title: "Configure em 15 minutos",
    desc: "Crie sua conta gratuitamente e conecte seu ERP (IXC, SGP, MK Solutions) ou importe via planilha CSV. Sem instalação, sem técnico, sem burocracia.",
    icon: Building2,
    color: "bg-blue-600",
    badge: "15 min",
  },
  {
    n: "2",
    title: "Consulte antes de ativar",
    desc: "Antes de instalar, consulte o CPF ou CNPJ. Em menos de 2 segundos receba: score de risco, histórico em outros provedores, equipamentos pendentes e sugestão de decisão.",
    icon: Search,
    color: "bg-blue-700",
    badge: "< 2 seg",
  },
  {
    n: "3",
    title: "Reduza até 70% das perdas",
    desc: "Provedores que consultam antes de ativar relatam redução de até 70% na inadimplência. O anti-fraude bloqueia migradores seriais automaticamente.",
    icon: TrendingUp,
    color: "bg-green-600",
    badge: "até 70%",
  },
].map((step, i) => (
  <div key={i} className="relative text-center px-8 py-10 group" data-testid={`step-${step.n}`}>
    {i < 2 && (
      <div className="hidden md:block absolute top-1/3 right-0 w-full h-0.5 bg-gradient-to-r from-transparent via-slate-200 to-transparent translate-x-1/2 z-0" />
    )}
    <div className="relative z-10">
      <div className={`w-16 h-16 ${step.color} rounded-2xl flex items-center justify-center mx-auto shadow-xl group-hover:scale-110 transition-transform`}>
        <step.icon className="w-7 h-7 text-white" />
      </div>
      <div className="w-8 h-8 bg-white border-2 border-blue-200 rounded-full flex items-center justify-center mx-auto -mt-3 relative z-20 shadow-sm">
        <span className="text-xs font-black text-blue-600">{step.n}</span>
      </div>
      {/* Badge de dado concreto */}
      <div className="inline-flex items-center gap-1 bg-green-50 border border-green-200 text-green-700 text-xs font-bold px-2.5 py-1 rounded-full mt-3">
        <Zap className="w-3 h-3" />
        {step.badge}
      </div>
      <h3 className="text-lg font-bold text-slate-900 mt-3 mb-2">{step.title}</h3>
      <p className="text-sm text-slate-500 leading-relaxed max-w-xs mx-auto">{step.desc}</p>
    </div>
  </div>
))}
```

---

## MUDANÇA 6 — Nova seção "Planos e Preços" (inserir APÓS a seção de funcionalidades, ANTES da seção de ERPs)

```tsx
{/* SEÇÃO PREÇOS */}
<section id="precos" className="py-20 bg-white">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div className="text-center mb-14">
      <div className="inline-flex items-center gap-2 text-blue-600 text-xs font-bold uppercase tracking-widest mb-3">
        <CreditCard className="w-4 h-4" />
        Simples e Transparente
      </div>
      <h2 className="text-3xl sm:text-4xl font-black text-slate-900">
        Planos para todo tamanho de provedor
      </h2>
      <p className="text-slate-500 mt-3 max-w-xl mx-auto">
        Comece grátis. Consultas na sua própria base são sempre gratuitas.
        Créditos para a rede colaborativa a partir de R$ 0,90/consulta.
      </p>
    </div>

    <div className="grid grid-cols-1 md:grid-cols-3 gap-6 max-w-5xl mx-auto">
      {[
        {
          name: "Gratuito",
          price: "R$ 0",
          period: "/mês para sempre",
          desc: "Para provedores pequenos que querem começar",
          color: "border-slate-200",
          btnClass: "border border-blue-600 text-blue-600 hover:bg-blue-50",
          badge: null,
          features: [
            "Consultas ilimitadas na sua base",
            "30 créditos ISP para testar a rede",
            "Anti-fraude básico",
            "Importação via CSV",
            "1 usuário",
          ],
          notIncluded: ["Créditos mensais inclusos", "Consulta em lote", "Integração automática ERP"],
        },
        {
          name: "Básico",
          price: "R$ 149",
          period: "/mês",
          desc: "Para provedores com até 1.000 clientes",
          color: "border-blue-600 ring-2 ring-blue-600 ring-offset-2",
          btnClass: "bg-blue-600 hover:bg-blue-700 text-white",
          badge: "Mais Popular",
          features: [
            "Tudo do Gratuito",
            "200 créditos ISP/mês inclusos",
            "50 créditos SPC/mês inclusos",
            "Anti-fraude com notificação WhatsApp",
            "Integração com 1 ERP",
            "3 usuários",
            "Suporte por chat",
          ],
          notIncluded: ["Consulta em lote", "Relatórios avançados"],
        },
        {
          name: "Profissional",
          price: "R$ 349",
          period: "/mês",
          desc: "Para provedores com mais de 1.000 clientes",
          color: "border-slate-200",
          btnClass: "border border-blue-600 text-blue-600 hover:bg-blue-50",
          badge: null,
          features: [
            "Tudo do Básico",
            "500 créditos ISP/mês inclusos",
            "150 créditos SPC/mês inclusos",
            "Todos os ERPs integrados",
            "Consulta em lote (até 500 CPFs)",
            "Relatórios e dashboards avançados",
            "Usuários ilimitados",
            "Suporte prioritário",
          ],
          notIncluded: [],
        },
      ].map((plan, i) => (
        <div
          key={i}
          className={`relative border rounded-2xl p-6 flex flex-col ${plan.color} transition-shadow hover:shadow-lg`}
          data-testid={`plan-${i}`}
        >
          {plan.badge && (
            <div className="absolute -top-3 left-1/2 -translate-x-1/2 bg-blue-600 text-white text-xs font-bold px-4 py-1 rounded-full">
              {plan.badge}
            </div>
          )}
          <div className="mb-5">
            <h3 className="text-lg font-bold text-slate-900">{plan.name}</h3>
            <div className="mt-2 mb-1">
              <span className="text-4xl font-black text-slate-900">{plan.price}</span>
              <span className="text-sm text-slate-400 ml-1">{plan.period}</span>
            </div>
            <p className="text-sm text-slate-500">{plan.desc}</p>
          </div>

          <ul className="space-y-2.5 mb-6 flex-1">
            {plan.features.map((f, fi) => (
              <li key={fi} className="flex items-start gap-2 text-sm text-slate-700">
                <span className="text-green-500 mt-0.5 flex-shrink-0">✓</span>
                {f}
              </li>
            ))}
            {plan.notIncluded.map((f, fi) => (
              <li key={fi} className="flex items-start gap-2 text-sm text-slate-400">
                <span className="text-slate-300 mt-0.5 flex-shrink-0">✗</span>
                {f}
              </li>
            ))}
          </ul>

          <Button
            className={`w-full font-bold ${plan.btnClass}`}
            onClick={goRegister}
          >
            {i === 0 ? "Criar Conta Grátis" : "Começar Agora"}
          </Button>
        </div>
      ))}
    </div>

    {/* Nota sobre créditos extras */}
    <div className="mt-10 text-center">
      <p className="text-sm text-slate-500">
        Precisa de mais créditos?{" "}
        <strong className="text-slate-700">Pacotes avulsos a partir de R$ 49,90</strong>{" "}
        (50 créditos ISP). Os créditos não expiram.
      </p>
      <div className="flex items-center justify-center gap-6 mt-4 text-xs text-slate-400">
        <span className="flex items-center gap-1">
          <Lock className="w-3 h-3" /> Sem contrato de fidelidade
        </span>
        <span className="flex items-center gap-1">
          <Shield className="w-3 h-3" /> Dados protegidos pela LGPD
        </span>
        <span className="flex items-center gap-1">
          <Zap className="w-3 h-3" /> Cancele quando quiser
        </span>
      </div>
    </div>
  </div>
</section>
```

---

## MUDANÇA 7 — Melhorar seção de ERPs

Substituir o subtítulo e parágrafo da seção de ERPs:
```tsx
// Atual:
<p className="text-slate-500 mt-3">Conecte seu sistema de gestão e comece a usar em minutos</p>

// Novo:
<p className="text-slate-500 mt-3">
  Integração em <strong className="text-slate-700">15 minutos</strong>. Sem programação. Sem técnico.
</p>
```

Adicionar ABAIXO do grid de logos dos ERPs:
```tsx
<p className="text-center text-sm text-slate-400 mt-6">
  Não usa ERP?{" "}
  <strong className="text-slate-600">Sem problema</strong> — importe sua base de inadimplentes via planilha CSV em qualquer formato.
</p>
```

---

## MUDANÇA 8 — Adicionar 3 novas perguntas no FAQ sobre economia

No array de FAQs, ADICIONAR estas 3 entradas ao final:
```tsx
{
  q: "Quanto economiza um provedor com 500 clientes?",
  a: "Um provedor com 500 clientes e taxa de inadimplência de 10% perde em média R$ 2.400 por mês em equipamentos não devolvidos e R$ 6.000 em receita não recebida. Com o Consulta ISP consultando antes de cada ativação, provedores relatam evitar entre 50% e 70% desses casos. O ROI do plano Básico (R$ 149/mês) se paga com apenas 1 calote evitado por mês.",
},
{
  q: "Funciona para provedor pequeno com menos de 200 clientes?",
  a: "Sim, especialmente para pequenos. Para um provedor com 150 clientes, o custo de uma única ONU perdida (R$ 280) representa muito mais proporcionalmente do que para quem tem 5.000. O plano Gratuito já é suficiente para começar — consultas na sua própria base são sempre gratuitas, sem limite.",
},
{
  q: "Quanto tempo leva para ver o primeiro resultado?",
  a: "Provedores relatam evitar o primeiro calote já na primeira semana de uso, simplesmente consultando o CPF antes de ativar. O anti-fraude começa a funcionar assim que você importa sua base de inadimplentes — e o alertas chegam em tempo real quando qualquer cliente seu com dívida tenta contratar em outro provedor da rede.",
},
```

---

## MUDANÇA 9 — Reescrever a seção CTA final

Substituir TODA a `<section className="py-20 bg-blue-600 relative overflow-hidden">` (a última seção antes do footer) por:

```tsx
<section className="py-20 bg-blue-600 relative overflow-hidden">
  <div className="absolute inset-0">
    <div className="absolute top-0 left-1/3 w-96 h-96 bg-blue-500/30 rounded-full blur-3xl" />
    <div className="absolute bottom-0 right-1/3 w-80 h-80 bg-blue-700/30 rounded-full blur-3xl" />
  </div>
  <div className="relative max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
    <div className="inline-flex items-center gap-2 bg-white/10 text-white text-xs font-bold uppercase tracking-widest px-3 py-1.5 rounded-full mb-6">
      <AlertTriangle className="w-3.5 h-3.5" />
      Seu próximo inadimplente já pode estar na base
    </div>
    <h2 className="text-3xl sm:text-4xl font-black text-white mb-4">
      Cada calote evitado paga o plano inteiro.
      <br />
      <span className="text-blue-200">Comece grátis agora.</span>
    </h2>
    <p className="text-lg text-blue-100 mb-8 max-w-2xl mx-auto">
      Cadastro em 2 minutos. Sem cartão de crédito. 30 créditos gratuitos para
      testar a rede colaborativa. Consultas na sua base são sempre gratuitas.
    </p>

    <div className="flex flex-col sm:flex-row items-center justify-center gap-4 mb-8">
      <Button
        size="lg"
        className="bg-white text-blue-700 hover:bg-blue-50 px-10 gap-2 h-12 text-base font-bold shadow-xl"
        onClick={goRegister}
        data-testid="button-cta-bottom"
      >
        Criar Conta Grátis
        <ArrowRight className="w-4 h-4" />
      </Button>
      <Button
        size="lg"
        variant="outline"
        className="border-white/30 text-white hover:bg-white/10 px-8 gap-2 h-12 text-base"
        onClick={goLogin}
        data-testid="button-login-bottom"
      >
        Já tenho conta — Login
      </Button>
    </div>

    {/* Garantias */}
    <div className="flex flex-col sm:flex-row items-center justify-center gap-6 text-sm text-blue-100">
      <span className="flex items-center gap-2">
        <span className="text-green-300">✓</span>
        Gratuito para sempre na base própria
      </span>
      <span className="flex items-center gap-2">
        <span className="text-green-300">✓</span>
        Sem contrato de fidelidade
      </span>
      <span className="flex items-center gap-2">
        <span className="text-green-300">✓</span>
        Dados protegidos pela LGPD
      </span>
    </div>
  </div>
</section>
```

---

## MUDANÇA 10 — Adicionar seção de números de impacto de mercado (após a seção "Como funciona", antes de "Funcionalidades")

Inserir ENTRE `</section>` do "como-funciona" e `<section id="funcionalidades" ...>`:

```tsx
{/* NÚMEROS DO MERCADO */}
<section className="py-16 bg-slate-900">
  <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div className="text-center mb-10">
      <p className="text-slate-400 text-sm font-semibold uppercase tracking-widest">
        O problema é real e crescente
      </p>
    </div>
    <div className="grid grid-cols-2 sm:grid-cols-4 gap-6">
      {[
        {
          stat: "19.000+",
          desc: "ISPs regionais no Brasil",
          sub: "63,8% do market share de banda larga",
          color: "text-blue-400",
        },
        {
          stat: "73 milhões",
          desc: "brasileiros negativados",
          sub: "Recorde histórico em 2025 (CNDL/SPC)",
          color: "text-red-400",
        },
        {
          stat: "R$ 400",
          desc: "custo médio de ativar um cliente FTTH",
          sub: "ONU + instalação + ativação",
          color: "text-amber-400",
        },
        {
          stat: "R$ 690",
          desc: "prejuízo médio por inadimplente",
          sub: "dívida + equipamento não devolvido",
          color: "text-orange-400",
        },
      ].map((item, i) => (
        <div key={i} className="text-center">
          <p className={`text-4xl sm:text-5xl font-black ${item.color} mb-2`}>
            {item.stat}
          </p>
          <p className="text-sm font-semibold text-white mb-1">{item.desc}</p>
          <p className="text-xs text-slate-400">{item.sub}</p>
        </div>
      ))}
    </div>
  </div>
</section>
```

---

## INSTRUÇÕES FINAIS

1. Adicionar `useState` para `calcClientes`, `calcTaxa` e `calcEquip` no início do componente (já há outros `useState` lá).

2. Garantir que os imports já existentes (`AlertTriangle`, `TrendingUp`, `Lock`, `Shield`, `CreditCard`, `Zap`, `ArrowRight`, etc.) estejam no import do topo. Se algum não estiver, adicionar.

3. A **ordem final das seções** deve ser:
   1. Nav
   2. Hero (reescrito)
   3. **Calculadora de Economia** ← NOVA
   4. Base Compartilhada (existente)
   5. **Anti-Fraude em Ação** ← NOVA
   6. Como Funciona (atualizado)
   7. **Números do Mercado** ← NOVA
   8. Funcionalidades (existente)
   9. **Planos e Preços** ← NOVA
   10. ERPs Integrados (copy atualizado)
   11. Por que escolher (existente)
   12. FAQ (com 3 novas perguntas)
   13. CTA Final (reescrito)
   14. Footer (existente)
   15. LandingChatbot (existente)

4. Não altere nenhum `data-testid` existente.

5. Rode o projeto para garantir que compila sem erros TypeScript.